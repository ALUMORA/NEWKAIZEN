// Catálogo de primitivas (C1). Solo en desarrollo: router.jsx no registra estas rutas en un
// build. C1 agrega aquí { path: route(PATHS.devUi), element: <DevUiPage />, handle: {...} } con
// la página cargada con lazy().
export const routes = []
