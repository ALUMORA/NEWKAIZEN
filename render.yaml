services:
  - type: web
    name: kaizen-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend.py
    plan: free
    envVars:
      - key: PORT
        generateValue: false
