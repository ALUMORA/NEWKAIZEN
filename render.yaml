services:
  # Servicio actual de la UI vieja: mismo arranque, ahora con FastAPI y las rutas v1 (KAIZEN_ENV
  # sin definir = development). No se toca hasta cortar la UI vieja.
  - type: web
    name: kaizen-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend.py
    plan: free
    envVars:
      - key: PORT
        generateValue: false

  # API v2 para el frontend nuevo: producción, con sesión obligatoria y sin rutas v1.
  - type: web
    name: kaizen-api
    runtime: python
    branch: analizavende
    autoDeploy: true
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend.py
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: KAIZEN_ENV
        value: production
      - key: AUTH_REQUIRED
        value: "true"
      - key: SECRET_KEY
        sync: false
      - key: USERS
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false
      - key: BANXICO_TOKEN
        sync: false
      - key: FRED_API_KEY
        sync: false
