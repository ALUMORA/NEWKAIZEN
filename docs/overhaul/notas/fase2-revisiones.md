# Fase 2: lo que encontraron los revisores independientes, SIN corregir

Generado el 22 de septiembre de 2026, al cerrar la fase con recorte por costo.

**Lee esto antes de construir encima de la fase 2.** Cada stream pasó por un revisor independiente
que volvió a correr sus compuertas y buscó defectos reales. La ronda de corrección se lanzó pero
**ningún agente corrector alcanzó a terminar**: el workflow se detuvo a propósito para no agotar la
cuota de la sesión. Algunos correctores sí dejaron commits o cambios en su worktree antes de morir, y
esos sí entraron al merge (se probaron uno por uno antes de aceptarlos), pero **no hay ningún stream
donde se pueda dar por cerrada la lista de abajo**.

Dos streams no alcanzaron revisión: **B3b** (valuación y momentum) y **B3c** (screeners). Ahí no hay
lista porque nadie los revisó, que es distinto de que estén limpios.

Severidades: `blocker` y `major` son las que hay que atender; `minor` es criterio de quien retome.

## A1

Veredicto de la corrida 1: **pass_with_issues** (3 blocker/major, 7 minor)

- **[major]** backtest.js: constantMix y buyAndHold devuelven el mismo typedef BacktestResult pero alinean 'dates' contra arreglos distintos, sin decirlo en el typedef. En buyAndHold, dates.length === values.length (dates y values se corresponden uno a uno). En constantMix, values tiene un elemento mas que dates, porque values arranca en 1 antes del primer periodo mientras dates son las fechas de llegada. Verificado ejecutando la libreria: constantMix({dates:['2026-01-31','2026-02-28'], values:{A:[.1,-.1]}}, {A:1}, 1) devuelve values [1, 1.1, 0.99] con dates ['2026-01-31','2026-02-28'], y buyAndHold({dates:[3 fechas], values:{A:[100,110,99]}}, {A:1}) devuelve values de 3 con dates de 3. Un consumidor de fase 3 que grafique con el zip natural dates.map((d,i)=>({d, v: values[i]})) obtiene la curva correcta para buyAndHold y para constantMix dibuja cada valor un periodo antes y pierde el ultimo: muestra 1 en 2026-01-31 y 1.1 en 2026-02-28 cuando lo correcto es 1.1 y 0.99. Ninguna prueba lo cubre porque ninguna compara dates.length contra values.length. Arreglo: unificar la semantica en el typedef y en las dos funciones. Lo mas barato sin cambiar buyAndHold es que constantMix exponga dos campos, 'dates' alineado con returns y 'valueDates' alineado con values (que solo se puede construir si el llamador pasa tambien la fecha de arranque, si no queda null), y que el typedef diga explicitamente con que arreglo se corresponde cada uno. Agregar una prueba por funcion que afirme la relacion de largos.
- **[major]** rates.js rfSeriesForDates: la fecha de CIERRE de cada periodo nunca se valida, y cuando no sirve la funcion inventa una tasa con el intervalo nominal en vez de devolver null. El bucle solo hace parseIsoDate(targetDates[i]) (indices 0..n-2); para el cierre usa daysBetween(targetDates[i], targetDates[i+1]), y si eso da null o <= 0 cae en fallbackDays = 365/periodsPerYear(interval). Verificado ejecutando: rfSeriesForDates({dates:['2026-01-01'],values:[.11]}, ['2026-01-08','no-es-fecha'], '1wk') devuelve [0.0021379242343320115]; lo mismo con el numero 42, con '2026-02-30' (fecha inexistente) y con fechas en orden descendente ['2026-01-15','2026-01-08']. El valor inventado es el de 365/52 = 7.019 dias, o sea 0.0021379, contra el verdadero de 7 dias que es 0.0021321: se parece lo suficiente como para que nadie lo note al ver la pantalla, y contamina Sharpe, Sortino y Treynor rio abajo. Es justo lo que el contrato v2 prohibe (nada de valores fijos silenciosos) en la unica funcion que existe para eso. Arreglo: validar parseIsoDate(targetDates[i+1]) y devolver null si falla; si el hueco no es estrictamente positivo devolver null en lugar de usar fallbackDays; con fechas ISO ascendentes y unicas (lo que garantiza alignPanel) fallbackDays nunca se usa, asi que se puede borrar junto con el parametro 'interval' o dejarlo solo documentado como inservible. Agregar casos de prueba con fecha final invalida, repetida y descendente.
- **[major]** backtest.js withBenchmark: la firma que fija el spec es withBenchmark(values, benchValues) con dos argumentos, pero la implementacion es withBenchmark(values, benchValues, { k }) sin valor por omision del objeto, asi que llamarla como dice el spec lanza TypeError: Cannot destructure property 'k' of 'undefined' as it is undefined. Verificado ejecutando withBenchmark([1,1.1],[1,1.05]) y withBenchmark([1,1.1],[1,1.05],null), las dos truenan; con {} si devuelve null. Rompe la regla propia de la libreria (una entrada mala devuelve null, no se propaga) y, en una pagina de fase 3, una excepcion no atrapada en render es pantalla en blanco mas console.error, que en este repo es defecto bloqueante. Arreglo: withBenchmark(values, benchValues, { k } = {}) y dejar que el guardia !isNum(k) ya existente devuelva null; o darle k = 1 por omision y documentar que sin k el TE y el IR salen por periodo. Mismo cuidado con regress(portExcess, benchExcess), que en la firma del spec no lleva k: ahi k = 1 por omision hace que el campo llamado alphaAnnual devuelva la alfa por periodo sin avisar.
- **[minor]** returns.js periodsPerYear: PERIODS es un objeto literal, asi que la busqueda cae al prototipo y devuelve miembros heredados en lugar de null. Verificado: periodsPerYear('constructor'), ('toString') y ('hasOwnProperty') devuelven una Function y ('__proto__') devuelve un objeto, cuando el JSDoc promete number|null. isInterval() sobre los mismos valores devuelve false, o sea que las dos funciones publicas se contradicen. El dano practico es acotado porque todo lo que consume k valida con isNum (el resultado termina siendo null igual), pero un llamador que haga 'const k = periodsPerYear(params.interval); if (k === null) return s/d' se salta el guardia con un parametro de URL como ?interval=toString. Arreglo: const PERIODS = Object.freeze(Object.assign(Object.create(null), {...})) o un guardia con Object.hasOwn(PERIODS, interval); y agregar esos cuatro nombres al caso de prueba de intervalos no reconocidos.
- **[minor]** fx.js convertSeries: el atajo 'if (from === to) return v' esta ANTES de cualquier validacion de moneda, asi que una moneda que la app no maneja pasa de largo. Verificado: convertSeries([100,200], null, 'EUR', 'EUR') devuelve [100,200] y convertSeries([100],[1],'BRL','BRL') devuelve [100], mientras que toCurrency(100,'EUR','EUR',null) devuelve null y el JSDoc de convertSeries promete 'null si alguna moneda no se reconoce'. O sea que la unica puerta que valida monedas en serie deja pasar una serie en euros como si estuviera validada, en una libreria cuya regla numero uno es no mezclar monedas. Arreglo: mover la comprobacion CURRENCIES.includes(from) && CURRENCIES.includes(to) arriba del atajo, igual que en toCurrency, y agregar la prueba con from === to y moneda desconocida.
- **[minor]** performance.js parametricVaR y parametricCVaR: no validan 'alpha'. Como el unico uso es '1 - alpha', una cadena se convierte sola y la funcion devuelve un numero. Verificado: parametricVaR(.045, .0591608, '0.95') devuelve 0.05231085645335068 y parametricCVaR(.045, .0591608, '0.9') devuelve 0.05882621715791453, mientras que historicalVaR([-.05,.1], '0.95') si devuelve null. Dos funciones hermanas del mismo modulo tratan la misma entrada sucia de forma distinta, y la regla escrita en _util.js dice que una cadena devuelve null. Un alpha que llega como string desde un query param o un JSON mal tipado se cuela. Arreglo: agregar 'if (!isNum(alpha) || alpha <= 0 || alpha >= 1) return null' en las dos, y extender la prueba de rechazo de entradas sucias que ya existe para las historicas.
- **[minor]** Varias funciones lanzan TypeError en vez de devolver null cuando el argumento que desestructuran viene ausente o en null, contra la regla escrita en la cabecera de _util.js. Verificado ejecutando: pnlDecomposition() y pnlDecomposition(null) truenan con 'Cannot destructure property quantity' (pnlDecomposition({}) si devuelve null); summary([.01,.02], null), alignPanel(serie, null), panelReturns(panel, null), rfSeriesForDates(s, d, i, null) y fxAt(s, d, null) truenan con 'Cannot read properties of null'. El caso realista es pnlDecomposition(posicion) donde 'posicion' vino undefined de una busqueda por simbolo. Arreglo: pnlDecomposition(position) validando 'if (!position || typeof position !== object) return null' antes de desestructurar, y poner '= {}' en los objetos de opciones de las otras cinco firmas.
- **[minor]** performance.js summary: 'best: Math.max(...r)' y 'worst: Math.min(...r)' expanden el arreglo como argumentos, asi que revientan el limite de argumentos del motor en series largas. Verificado: summary(n=100000, {k:252}) funciona y summary(n=200000, {k:252}) lanza RangeError: Maximum call stack size exceeded. Para la serie diaria de un instrumento (unos 2520 puntos en 10 anios) no pasa nada, pero summary es un export publico del barril y las series de salida de A3 (Monte Carlo) o de un walk-forward largo pueden pasarse. Una libreria que promete null en vez de NaN no deberia tener una excepcion latente. Arreglo: reemplazar el spread por un bucle de un paso que calcule maximo, minimo y el conteo de positivos junto con los demas acumulados.
- **[minor]** Hueco de cobertura en drawdowns: la recuperacion no esta probada en el caso del empate exacto. Verificado con mutacion en una copia del arbol: cambiar 'if (w[i] >= w[worstPeak])' por 'if (w[i] > w[worstPeak])' en performance.js deja las 456 pruebas de src/lib/finance en verde (fue el unico sobreviviente de 22 mutantes). Los datos usados no lo tocan: en W = [100,120,90,110,80,130] la recuperacion ocurre en 130, estrictamente arriba de 120, y el caso 'sin recuperacion' termina en 95. El comportamiento actual es el correcto, pero nada lo protege de una regresion, y con '>' una cartera que vuelve exactamente a su maximo reportaria recoveryIndex null y una durationPeriods mas larga de lo real. Arreglo: agregar drawdowns([100,120,90,120]) esperando recoveryIndex 3 y durationPeriods 2 (lo confirme corriendo la version actual).
- **[minor]** backtest.js buyAndHold no valida 'pricePanel.dates': hace Array.isArray(...) y luego slice(0, panel.length) sin comprobar el largo ni que sean fechas ISO. Verificado: buyAndHold({dates:['2026-01-31'], values:{A:[100,110,99]}}, {A:1}) devuelve values de largo 3 con dates de largo 1, y buyAndHold({dates:['no','es','fecha'], ...}) devuelve dates ['no','es','fecha'] tal cual. constantMix si valida el largo, pero solo cuando se pide rebalanceo por calendario. El resultado es un BacktestResult que aparenta traer calendario y no lo trae, y es el mismo objeto que consumira la pagina de backtest. Arreglo: devolver null si pricePanel.dates existe y su largo no es igual al de las series, y validar cada fecha con el mismo parseIsoDate que ya usa calendarBucket; si dates no viene, devolver null en el campo como ya hace hoy.

Veredicto de la corrida 2: **fail** (2 blocker/major, 5 minor)

- **[blocker]** EL CÓDIGO REVISADO NO ESTÁ COMMITEADO, Y LO QUE SÍ ESTÁ EN ws/A1 ESTÁ ROTO. 'git status --porcelain' lista 14 archivos modificados (447 inserciones) con mtimes de 3.5 horas después del último commit; el stream declaró el árbol limpio. Importando directamente los módulos de HEAD (git show HEAD:src/lib/finance/*.js) comprobé que la versión commiteada: (1) withBenchmark(values, benchValues) con dos argumentos, que es la firma del spec y la que el propio handoff documenta, lanza 'TypeError: Cannot destructure property k of undefined'; (2) pnlDecomposition() lanza TypeError en vez de devolver null; (3) summary(returns) sin opciones supone k=252 en silencio, justo la constante de anualización escondida que el spec prohíbe, y devuelve cagr 41.47 y sharpe 33.67 para una serie de 2 periodos; (4) rfSeriesForDates con una fecha no ISO o con el calendario desordenado cae en los días nominales del intervalo y devuelve una tasa inventada (.0021379 en vez de .0021321) en lugar de null, que contamina Sharpe, Sortino y Treynor; (5) constantMix no trae valueDates y su 'dates' significa algo distinto que el de buyAndHold, así que el zip natural corre la gráfica un periodo. El árbol de trabajo corrige las cinco cosas y pasa todas las compuertas (857 pruebas, no 829). FIX: commitear el árbol de trabajo en ws/A1 antes del merge (DEVELOPER_DIR=/Library/Developer/CommandLineTools git -C '.../05 NEWKAIZEN.wt/A1' add -A && git commit) y volver a correr lint, typecheck, test, check y check-ownership sobre ese commit. El resto del entregable está sano: es una acción, no un rehacer.
- **[major]** buyAndHold y constantMix tiran en silencio el peso de un símbolo que no está en el panel y reparten ese peso entre los demás. Verificado: buyAndHold({values:{A:[1,1.1],B:[1,1]}}, {A:.5,B:.3,C:.2}) corre un backtest de dos activos a 62.5/37.5 y no deja ninguna señal de que el 20 % de C desapareció; constantMix hace lo mismo. La ruta realista es directa: alignPanel descarta un símbolo por falta de historia y lo reporta en 'dropped', el llamador le pasa los pesos objetivo originales y la gráfica sale plausible y equivocada, con el agravante de que el activo descartado suele ser el más volátil. La culpa está en normalizedWeights (src/lib/finance/_util.js), que solo lee las llaves que le piden y normaliza. FIX: en normalizedWeights, o en buyAndHold y constantMix antes de llamarla, devolver null cuando Object.keys(weights) tenga alguna llave que no esté en 'symbols'; si se prefiere no romper al llamador, agregar 'ignored: string[]' a BacktestResult, documentarlo en el typedef y que la interfaz lo muestre.
- **[minor]** normalizedWeights voltea el signo cuando los pesos suman negativo, así que un error de captura se vuelve un portafolio largo con pinta de bueno. Verificado: constantMix({values:{A:[0.1],B:[0]}}, {A:-0.5,B:-0.5}, 1) devuelve weights {A:.5238,B:.4762} y un rendimiento positivo. La guarda actual solo rechaza Math.abs(total) < EPS, no un total negativo. La app es solo largos. FIX: en src/lib/finance/_util.js, cambiar la guarda a 'if (total < EPS) return null' y rechazar además cualquier w individual negativo, o documentar explícitamente en el JSDoc que la función es para pesos no negativos.
- **[minor]** normalInvCdf no cumple el 1e−11 contra scipy que declara el JSDoc ('queda al nivel del doble') ni el handoff, y el paso de Halley EMPEORA la cola respecto a Acklam solo. Medido contra scipy.stats.norm.ppf en 130 valores: 2.5e−13 o mejor en p ∈ [1e−4, 1−1e−4], pero 3.4e−11 en p=1e−6, 1.7e−7 en p=1−1e−10 y 4.6e−5 en p=1−1e−12, cuando Acklam crudo es ~1e−9 relativo en todo el rango. La causa es normalCdf: en la cola superior calcula 1 − Φ(−z), que no tiene precisión relativa ahí, así que el residual 'cdf − p' del refinamiento es ruido. Los casos golden de normalInvCdf solo van de p=.001 a p=.999, por eso nada lo detecta. Impacto práctico bajo (los alfas de VaR reales viven en [.90,.9999], donde está perfecto), pero la afirmación es falsa y la prueba no la respalda. FIX: en src/lib/finance/stats.js, saltarse el refinamiento cuando p < 1e−6 || p > 1 − 1e−6 (o cuando Math.abs(x) > 4.75) y devolver el Acklam crudo; acotar el JSDoc y el handoff al rango realmente verificado; agregar casos golden en p = 1e−8 y p = 1 − 1e−8 para que la promesa quede amarrada a una prueba.
- **[minor]** summary reporta maxDrawdown null para un portafolio que se va a cero, que es justo el caso donde el número es seguro. Verificado: summary([-1, 0.1], {k:12}) devuelve {maxDrawdown: null, calmar: null, cagr: null} con vol 2.69, sharpe −2.00 y worst −1 llenos. El motivo es que drawdowns() rechaza cualquier valor que no sea estrictamente positivo, y cumulative() produce un 0 cuando hay un periodo de −100 %. La interfaz acabaría mostrando 's/d' en la caída máxima de una ruina total. FIX: en src/lib/finance/performance.js, relajar la guarda de drawdowns a 'rechazar solo valores negativos y exigir w[0] > 0' (con w[0] > 0 el pico nunca es 0, así que w[i]/peak − 1 sigue bien definido y [1,0] da maxDrawdown −1); dejar null solo para valores negativos, y ajustar el JSDoc.
- **[minor]** foreignExposure cuenta como extranjera a una posición sin campo currency, o sea convierte un dato ausente en una afirmación. Verificado: foreignExposure([{value:100,currency:'MXN'},{value:50}], 'MXN') devuelve .3333, y la pantalla diría '33 % en moneda extranjera' cuando la verdad es que no se sabe. exposureBy sí hace lo correcto (manda esa posición al grupo key:null para que se dibuje 's/d'), pero foreignExposure compara group.key !== baseCurrency y el null cae del lado extranjero. FIX: en src/lib/finance/risk.js, devolver null si algún grupo trae key === null, o cambiar la firma a {foreign, unknown} para que el dato faltante viaje aparte; documentarlo en el JSDoc.
- **[minor]** inferInterval acepta un calendario desordenado y contesta '1d'. Verificado: inferInterval(['2026-03-01','2026-02-01','2026-01-01']) devuelve '1d' a partir de una mediana de huecos de −28 días. El JSDoc pone el orden ascendente como precondición, pero el módulo ya tiene ascendingIsoDates() en _util.js (se agregó en estos mismos cambios sin commitear) y el resto de la librería sí valida. Un '1d' falso se convierte en k=252 y de ahí en una volatilidad anualizada inflada ~2.2 veces. FIX: en src/lib/finance/returns.js, arrancar inferInterval con 'if (ascendingIsoDates(dates, 2) === null) return null' y agregar el caso a returns.test.js.

## A2

Veredicto de la corrida 1: **pass_with_issues** (2 blocker/major, 6 minor)

- **[major]** src/lib/finance/walkforward.js:67, `maxDrawdownOf` arranca el pico en `values[0]` en vez de en 1, la riqueza inicial, así que la caída que empieza en el PRIMER periodo fuera de muestra no se cuenta. `values` ya excluye el 1 inicial (se llena después de aplicar cada rendimiento), o sea que el pico de arranque se pierde. Verificado así: construí un panel donde el primer periodo fuera de muestra cae 30 % y el resto sube poco; `summary.maxDrawdown` sale 0 cuando la caída real es −0.30. Luego corrí 300 walk-forward simulados (T=200, N=4, ventana 104, tramos de 13, minVariance): 23 (7.7 %) reportan mal, el peor por 3.6 puntos porcentuales (−0.3016 reportado contra −0.3371 real). El golden NO lo atrapa porque scripts/golden/walkforward_golden.py:161 comete el mismo error (`running_peak = np.maximum.accumulate(values)` sobre la misma serie recortada), y la única prueba de la caída es `expect(r.summary.maxDrawdown).toBeLessThanOrEqual(0)` en walkforward.test.js:165. Es justo el número que el proyecto no se puede permitir subestimar: el panel de walk-forward enseñaría "caída máxima 0 %" de una estrategia que perdió 30 % de entrada. Arreglo: en walkforward.js cambiar `let peak = values[0]` por `let peak = 1` (probé el parche sobre una copia: 0 de 300 corridas mal y el caso extremo pasa a −0.30), y espejarlo en el golden con `np.maximum.accumulate(np.concatenate([[1.0], values]))[1:]`, luego regenerar walkforward.json. Ojo además al deduplicar contra `performance.js::drawdowns` de A1: esa función recibe una serie de valores que SÍ incluye el arranque (el caso del spec W=[100,120,90,...] da MDD −.333333), así que al cambiarla hay que pasarle [1, ...values] o el error sobrevive al merge.
- **[major]** src/lib/finance/expected.js:104, la contracción de `jamesStein` depende en silencio de la periodicidad de `means` y `cov`, y la documentación del propio A2 empuja a la combinación equivocada. La fórmula es w = lambda/(T+lambda) con lambda = (n+2)/d y d = diff' Σ⁻¹ diff; si pasas mu y Σ anualizados, d se multiplica por k y lambda se divide entre k, o sea que w sale como si tuvieras k·T observaciones. Medido con un panel semanal de T=156 y N=5: con `perPeriod` y la Σ por periodo la contracción es 0.613928, y con `historicalMean(...).mu` y `annualize(cov, 52)` es 0.029673, 20.7 veces menos. El JSDoc solo pide "todas en la misma periodicidad", que anual+anual cumple, y la regla general del handoff dice literalmente "todo anual, o todo por periodo", además de que la receta que A2 le deja a F4 es `ledoitWolf` → `annualize(cov, k)` → optimizador, todo anual. Con eso el estimador prácticamente no encoge y la pantalla mostraría medias históricas casi crudas bajo la etiqueta "James-Stein". Arreglo: documentar en el JSDoc y en el handoff que `means` y `cov` van POR PERIODO, en la misma periodicidad en que se contaron los T (y que `historicalMean` ya devuelve `perPeriod` justo para eso); o, mejor, recibir `k` y hacer la cuenta internamente por periodo devolviendo mu en la unidad que entró. Agregar una prueba que corra el mismo panel en las dos unidades y exija la misma contracción.
- **[minor]** src/lib/finance/optimize.js:625, `riskParity` no converge cuando la covarianza está mal condicionada y devuelve contribuciones de riesgo negativas, que no significan nada. Con cond(Σ)=1e10 en n=6 las contribuciones salen [0.2302, 0.1953, −0.1852, 0.2985, 0.2301, 0.2310] (dispersión 0.48) y con 1e12 salen [0.805, −0.896, −2.503, 0.796, −1.045, 3.843]. El spec pide contribuciones iguales dentro de 1e-8. A favor del stream: `converged` sale false en los dos casos y con todo lo realista que probé (sampleCov de rango deficiente con T=10 y N=20, Ledoit-Wolf con delta 0.87 y 1.0) converge en 20 a 24 iteraciones con dispersión 1e-17, así que no es un riesgo del día a día. Lo que falta es que el JSDoc diga que hay que revisar `converged` antes de mostrar `riskContributions`, y que el descenso coordinado no acepte devolver contribuciones negativas: o se cae a `null` o se cambia a la formulación logarítmica de Spinu, que es la que aguanta el mal condicionamiento.
- **[minor]** src/lib/finance/covariance.js:188, el valor de `shrinkage` que devuelve `ledoitWolfConstantCorrelation` con N=2 es arbitrario, y el handoff lo describe mal. Con N=2 el objetivo F coincide con S, gamma es cero o casi, y delta queda 0/0. El handoff afirma "yo devuelvo 0", pero midiéndolo con cuatro paneles de 2 activos obtuve delta 1, 0, 1 y 0 según cómo redondee gamma (T=5 → 1, T=20 → 0, T=60 → 1, T=200 → 0). La matriz sí es la misma en todos los casos (verificado contra PyPortfolioOpt, diferencia ≤ 1.1e-19), así que no afecta al optimizador, pero una pantalla que muestre "contracción aplicada: 100 %" o "0 %" para un portafolio de dos posiciones estaría enseñando un número que salta por redondeo. Arreglo: con N=2 devolver `shrinkage: null` (o 0 fijo y documentado como "no aplica") en vez de dejarlo salir de la división, y corregir esa línea del handoff.
- **[minor]** src/lib/finance/walkforward.js:86, cuando `method` es una función, los pesos que devuelve solo se revisan por largo y por ser números finitos: no se comprueba que sumen 1 ni que respeten `l` y `u`. Una estrategia que devuelva [0.8, 0.8] produce rendimientos fuera de muestra apalancados sin un solo aviso, y el resumen (acumulado, volatilidad, caída) sale igual de mal. La prueba de walkforward.test.js:184 que verifica la caja solo cubre los métodos con nombre. Arreglo: proyectar el resultado con `projectBoxSimplex(raw, cfg.l, cfg.u)` y dejar `note` diciendo que se ajustó, o lanzar `InvalidInputError` si |Σw − 1| > 1e-9, y documentar cuál de las dos es.
- **[minor]** src/lib/finance/optimize.js y linalg.js aceptan en silencio una covarianza no simétrica. `assertSquare` solo revisa que sea cuadrada, y `minVariance`, `meanVariance`, `efficientFrontier`, `maxSharpe`, `riskParity` y `quadForm` la usan tal cual. Para una Σ asimétrica el gradiente que usa FISTA (Σy) no es el gradiente de ½wᵀΣw, así que el resultado no es el óptimo de nada y no hay forma de que el consumidor se entere. Hoy nadie la produce asimétrica (sampleCov y Ledoit-Wolf simetrizan), pero F4 va a poder pasar matrices editadas a mano o venidas del API. Arreglo barato: en `prepare` llamar a `symmetrize`, o bien rechazar con `InvalidInputError` si `isSymmetric(cov, 1e-8)` es falso. `cholesky` ya hace la revisión y devuelve null; el optimizador no.
- **[minor]** Desvío del spec en el objetivo por omisión de `jamesStein`. El spec dice "jamesStein(means, cov, T) shrink toward grand mean", y la implementación encoge por omisión hacia el rendimiento del portafolio de mínima varianza (Jorion 1986), dejando el promedio simple detrás de `{target: 'average'}`. Está documentado en el código y en el handoff y es la variante defendible, pero cambia el número por omisión que van a ver las F-streams respecto a lo escrito en finance-spec.md. Verificado: con n=5 y T=156 el objetivo sale 0.16993 (mínima varianza) y no el promedio de las medias. Arreglo: que el orquestador decida y actualice el spec, o invertir el valor por omisión.
- **[minor]** El costo de `walkForward` con `maxSharpe` a n=40 no está medido en el reporte y sí molesta en el hilo principal: 725 ms con 260 semanas, ventana 104 y 12 cortes (medido por mí), contra los 324 ms que el stream declara para n=20. La frontera de 30 puntos a n=40 cuesta 114 ms y `maxSharpe` 49 ms. El spec fija n ≤ 40 como tamaño real, o sea que el caso peor es alcanzable desde la pantalla del optimizador. No es un defecto de corrección, pero el handoff que va a leer F4 solo trae los números de n=20 y le va a quedar corto el presupuesto: conviene agregar la fila de n=40 y dejar dicho que arriba de ~25 activos `walkForward` con tangente pide estado de carga o worker.

Veredicto de la corrida 2: **pass_with_issues** (1 blocker/major, 4 minor)

- **[major]** maxSharpe no es el máximo de la razón de Sharpe cuando ningún portafolio factible le gana a rf, y devuelve ese resultado etiquetado como tangente. src/lib/finance/optimize.js, función maxSharpe. El barrido solo recorre la rama tau >= 0, o sea la frontera de mínima varianza. Con exceso máximo negativo el óptimo de (mu_p - rf)/sigma_p está en la rama INEFICIENTE (conviene MÁS volatilidad para el mismo rendimiento), que esa parametrización nunca visita. CÓMO LO VERIFIQUÉ: (a) contra scipy SLSQP multi-arranque, caso n=10 con u=.2, rf=.05 y rendimiento máximo factible .018159 (exceso -.031841): JS devuelve Sharpe -0.54227757 con vol .058718 y SLSQP encuentra -0.42101008 con vol .094776, o sea el JS queda 29% peor y escoge menos riesgo justo donde conviene más. (b) Por la ruta real del producto: walkForward(X, dates, {method:'maxSharpe', rf: 0.10/52}) sobre un panel semanal de 200x5 con un tramo bajista cayó en ese régimen en 4 de 12 cortes, y en el corte 4 (2023-01-02) el JS dio -0.153430 contra -0.117395 del mejor portafolio hallado con 200 mil sorteos Dirichlet más los vértices, o sea 30.7% peor. Con CETES al 10% y mu por promedio histórico de ventana, el régimen es corriente en un mercado a la baja. (c) Ninguna prueba lo cubre: los 9 casos golden de maxSharpe tienen max(mu) entre .0779 y .1650 contra rf = .04, todos con exceso positivo. ARREGLO: en maxSharpe, calcular el rendimiento máximo factible con greedyLinear(m, lo, hi) (ya existe en el archivo) y si muᵀw_max - rf <= 0 devolver null, que ya está documentado como valor de retorno. walkforward.js::strategyWeights ya tiene la rama de respaldo escrita para ese null: 'No hubo portafolio tangente, se usó mínima varianza.' Si se prefiere devolver algo, que lleve una bandera explícita, pero nunca en silencio como el tangente.
- **[minor]** riskParity reporta converged=false en respuestas correctas y quema las 10000 iteraciones para nada. src/lib/finance/optimize.js, función riskParity. El criterio de paro es el paso relativo delta <= tol con tol = 1e-15, que está por debajo del piso de ruido de punto flotante, así que en muchas matrices sanas nunca se cumple. CÓMO LO VERIFIQUÉ: en un fuzz de 800 covarianzas realistas con estructura de factores (muestral y Ledoit-Wolf, n de 3 a 22), 118 devuelven converged=false aunque las contribuciones de riesgo son exactas (desviación máxima 0.00e+0 contra el objetivo de 1e-8 del spec). El caso realista de n=40 (Ledoit-Wolf de 200x40 anualizada) consume las 10000 iteraciones y 25.5 ms para entregar contribuciones exactas a 8.7e-17; la misma matriz muestral converge en 26 iteraciones y 0.1 ms. Un consumidor que condicione sobre converged rechazaría respuestas correctas. ARREGLO: parar sobre el residual que de verdad define la paridad, max_i |RC_i - b_i| <= 1e-12, o subir tol a un valor alcanzable (1e-12) y agregar un piso absoluto al denominador del paso.
- **[minor]** walkForward tira la bandera converged de los tres optimizadores, así que un corte que topó con maxIter no se distingue de uno que resolvió limpio. src/lib/finance/walkforward.js, función strategyWeights: usa minVariance(...).weights, rp.weights y ms.weights y nunca lee .converged, de modo que el rebalance sale con note: null. El campo note existe justo para eso y ya se usa en las otras dos rutas de respaldo. CÓMO LO VERIFIQUÉ: leyendo strategyWeights y comprobando que riskParity sobre una covarianza de cond 1e8 devuelve contribuciones de riesgo desviadas hasta 0.606 (con n=5, objetivo 0.2) y converged=false, y que walkForward conserva esos pesos sin nota. Con Ledoit-Wolf esa condición no se alcanza, por eso es menor y no mayor. ARREGLO: cuando el optimizador devuelva converged=false, poner en note algo como 'El optimizador no convergió en este corte, los pesos son aproximados.'
- **[minor]** largestEigenvalue declara convergencia en la primera iteración con matrices cuyo lambda-max está por debajo de ~1e-14, y minVariance devuelve entonces un portafolio muy equivocado. src/lib/finance/linalg.js, largestEigenvalue: el criterio es Math.abs(rayleigh - lambda) <= tol * Math.max(1, Math.abs(rayleigh)) con lambda inicializada en 0 y tol = 1e-14, o sea que el Math.max(1, ...) vuelve la tolerancia ABSOLUTA en vez de relativa a la escala de la matriz. CÓMO LO VERIFIQUÉ: barrido de escala sobre Sigma = diag(s, 4s). Con s >= 1e-12 todo sale exacto (w1 = 0.799999999999998, error 2e-15). Con s = 1e-15 la iteración de potencia para en la iteración 1 reportando lambda-max 1.600e-15 en vez de 4.000e-15 y converged=true; lipschitzOf devuelve ese valor, el paso 1/L queda 2.5 veces grande (paso por lambda-max = 2.5 > 2, o sea fuera del rango estable de FISTA) y minVariance devuelve w1 = 0.285851997826310 en vez de 0.8, con error 0.514 y converged=false. jacobiEigen sí da los valores correctos a esa escala, así que el problema es solo el criterio de paro de la iteración de potencia. No es alcanzable con datos de rendimientos (haría falta sigma del orden de 1e-7), pero largestEigenvalue es un export público que otros streams pueden llamar sobre matrices que no son covarianzas. ARREGLO: escalar la tolerancia por la magnitud de la matriz (la variable scale ya se calcula tres líneas arriba) y exigir al menos dos iteraciones antes de declarar convergencia.
- **[minor]** El objetivo por omisión de jamesStein no es el que dice el spec. finance-spec.md sección A2 dice 'jamesStein(means, cov, T) shrink toward grand mean', y src/lib/finance/expected.js usa por omisión target='minVariance', o sea mu0 = (1ᵀΣ⁻¹mu)/(1ᵀΣ⁻¹1), que es el rendimiento del portafolio de mínima varianza; el promedio simple queda detrás de target:'average'. La elección es defendible (es la de Jorion 1986) y está documentada en el JSDoc, pero quien programe contra el spec obtiene otro estimador. CÓMO LO VERIFIQUÉ: leyendo la línea del spec contra la firma y el cuerpo de jamesStein, y confirmando en tests/golden/expected.json que hay casos para los dos objetivos. Apunte relacionado, no es defecto porque el propio guion lo declara en su docstring: scripts/golden/expected_golden.py reimplementa la MISMA fórmula en numpy, así que ese golden valida el camino numérico (Cholesky propio contra LU de numpy, a 1e-12) y no la definición del estimador; James-Stein es el único de los cinco módulos sin referencia externa. ARREGLO: alinear el valor por omisión con el spec, o corregir la línea del spec para que diga que el objetivo por omisión es el portafolio de mínima varianza.

## A3

Veredicto de la corrida 1: **pass_with_issues** (0 blocker/major, 6 minor)

- **[minor]** src/lib/finance/montecarlo.js linea 644: el campo `seed` que regresa `simulate` no reproduce la corrida cuando se pasa `seed: null`. `createRng(null)` cae en DEFAULT_SEED y usa 'kaizen' (rng.js linea 96), pero el resultado reporta `String(seed)`, que es la cadena 'null'. Como lo verifique: corri `simulate({..., seed: null})`, tome el `seed` que reporta y volvi a simular con el; el saldo mediano cambio de 494232.1631459808 a 500156.1931108047. Es un hoyo de reproducibilidad justo en el campo que F4 va a guardar y mostrar para repetir un escenario, y un campo de formulario vacio llega como null con toda naturalidad. Arreglo: crear el generador antes de armar el resultado y devolver su etiqueta, o sea `const rng = createRng(seed)` (ya existe en la linea 533) y cambiar `seed: String(seed)` por `seed: rng.seed`. Una linea, y de paso deja el campo consistente con numeros, que hoy ya funcionan porque String(42) y createRng(42).seed son los dos '42'.
- **[minor]** src/lib/finance/montecarlo.js lineas 485 a 491: la validacion de `stepsPerYear` multiplo de 12 se dispara aunque la aportacion sea cero, porque `contributionFrequency` vale 'monthly' por omision. Como lo verifique: `simulate({ years: 10, stepsPerYear: 1, mu: 0.08, sigma: 0.15, paths: 10 })`, sin aportacion de ningun tipo, lanza 'montecarlo: con aportación mensual, stepsPerYear (1) tiene que ser múltiplo de 12'. El propio golden lo esquiva poniendo contributionFrequency 'annual' en el caso de pasos anuales. Cualquier feature que quiera pasos anuales, trimestrales o semanales para dibujar un saldo sin aportaciones se topa con un error que no tiene nada que ver con lo que pidio. Arreglo: saltarse el chequeo cuando `contribution === 0`, es decir `if (contribution !== 0 && k % perYear !== 0) throw ...`, y dejar `every = Math.max(1, Math.round(k / perYear))` para ese caso, que con aportacion cero no cambia ningun numero.
- **[minor]** src/lib/finance/montecarlo.js lineas 512 a 530: con `method: 'bootstrap'` los parametros `mu` y `sigma` nunca se validan, porque solo se leen dentro de la rama lognormal. Como lo verifique: `simulate({ method: 'bootstrap', history: [...], mu: Number.NaN, sigma: -5, ... })` no lanza y devuelve un resultado normal (terminal.p50 = 1143.59). Contradice el contrato que el propio modulo documenta en el JSDoc de `simulate` ('Lanza ... si algun parametro no es finito') y lo que dice el handoff ('Lanza cuando: algun numero no es finito ... sigma < 0'), ademas de la regla general del spec de rechazar NaN. Si F4 arma un solo objeto de opciones y solo cambia `method`, un `mu` roto se cuela sin aviso y reaparece en el momento en que el usuario regrese a lognormal. Arreglo: subir `finite(mu, 'mu')`, `finite(sigma, 'sigma')` y `if (sigma < 0) throw` al bloque de validacion de arriba (junto a las lineas 465 a 478), antes de ramificar por metodo.
- **[minor]** src/lib/finance/goals.js lineas 162 a 172: `requiredContribution` calcula y valida `maxContribution` DESPUES del retorno temprano por aportacion cero, asi que un `maxContribution` invalido pasa callado cuando la meta ya se cumple sin aportar. Como lo verifique: `requiredContribution({ target: 1, years: 1, initial: 1e9, mu: 0, sigma: 0, contributionFrequency: 'annual', stepsPerYear: 1, paths: 1, maxContribution: -5 })` devuelve {contribution: 0, probability: 1, bounded: false} sin lanzar, mientras que el mismo llamado con una meta inalcanzable si lanza 'goals: maxContribution tiene que ser mayor que cero'. Es exactamente la misma clase de defecto de orden que el stream ya corrigio en la validacion de `history`, y hace que un error de captura de F4 aparezca o no segun los datos. Arreglo: mover el calculo de `cap` y el `if (cap <= 0) throw` arriba del `const zero = run(0)`.
- **[minor]** src/lib/finance/goals.js lineas 191 a 204: cuando la biseccion se queda sin iteraciones devuelve `bounded: false` y una `contribution` que no convergio a `tolerance`, sin ninguna senal de que la respuesta esta sin refinar. Como lo verifique: con `maxIterations: 3` sobre una meta de 5,000,000 a 20 anos devolvio {contribution: 10416.67, probability: 0.56, iterations: 3, bounded: false}, mientras que con `maxIterations: 200` la respuesta convergida es {contribution: 9924.82, probability: 0.50, iterations: 23}. Es una sobrestimacion de 5 % presentada como resultado final. Con el valor por omision de 80 rara vez se llega ahi, pero el open_issue numero 3 del stream ya senala que `bounded` mezcla casos; conviene cerrarlo de una vez. Arreglo: agregar `converged: hi - lo <= tol` al objeto que regresa (y documentarlo), o partir `bounded` en los tres estados reales: tope insuficiente, iteraciones agotadas buscando el corchete, iteraciones agotadas bisecando.
- **[minor]** src/lib/finance/montecarlo.js lineas 465 a 477: `initial` se valida como no negativo pero `contribution` no, y una aportacion negativa produce numeros sin sentido en silencio. Como lo verifique: `simulate({ initial: 100000, contribution: -200000, years: 1, stepsPerYear: 12, mu: 0, sigma: 0, contributionGrowth: 0, paths: 2 })` devuelve saldo 0 desde el primer paso (el piso `next > 0 ? next : 0` de la linea 583 se traga el faltante, sin marcar que el plan es imposible) y `contributedTotal: -2300000`, que si se pinta como 'lo que llevas aportado' es basura. El modulo no ofrece retiros como funcion, asi que hoy no hay un uso legitimo del valor negativo. Arreglo: lanzar con `if (contribution < 0) throw new Error('montecarlo: contribution no puede ser negativa')`, o, si se quieren retiros de verdad, documentarlos y marcar el paso en que el saldo no alcanza en vez de aplanarlo a cero.

Veredicto de la corrida 2: **pass_with_issues** (2 blocker/major, 2 minor)

- **[blocker]** El branch ws/A3 NO contiene el trabajo que el stream describe. Hay 3 archivos modificados sin commitear en /Users/luisalfredolizarragasanchez/Desktop/CLAUDE/05 NEWKAIZEN.wt/A3 (src/lib/finance/goals.js, src/lib/finance/montecarlo.js, src/lib/finance/montecarlo.test.js, 117 líneas), y el stream declaró 'git status --porcelain quedó sin una sola modificación'. Si el orquestador mergea ws/A3 tal como está commiteado, se pierden arreglos reales. Lo comprobé sacando los archivos de HEAD con 'git show HEAD:...' a un directorio aparte y corriéndolos: (1) simulate({seed: null}) devuelve seed: 'null' y volver a pasar ese valor da OTRA corrida (p50 1050.5969 contra 1025.0582), o sea que el campo que F4 guardaría para repetir un escenario no reproduce nada; (2) contribution: −500 se acepta y deja contributedTotal en −11,000 con el saldo pisado en 0, sin avisar que el plan es imposible; (3) requiredContribution con maxIterations bajo devuelve bounded: false y una aportación sin refinar (3,819.44 contra 2,758.74 refinada) sin ninguna bandera que lo distinga de una respuesta buena; (4) mu y sigma no se validan con method 'bootstrap', así que un NaN se cuela callado; (5) stepsPerYear: 1 sin aportación truena pidiendo múltiplo de 12; (6) maxContribution negativo solo lanza a veces. El árbol de trabajo arregla los seis y agrega los campos converged y reason. Arreglo concreto: commitear esos 3 archivos en ws/A3 (dos commits, uno de fix y uno de test, mensajes de una línea en español sin trailer) y volver a correr npm run test para dejar el conteo real (506 en total, 133 de A3) en las notas. Mientras no se commiteen, las cifras declaradas (504 y 131) tampoco corresponden al branch.
- **[major]** src/lib/finance/montecarlo.js, probabilityAbove devuelve 1 (100 % de probabilidad de llegar a la meta) en vez de null cuando el búfer de terminalSorted ya fue transferido a un worker. Verificado: simulé 1,000 trayectorias, probabilityAbove(150000) dio 0.406; después de structuredClone(payload, { transfer }) (que es exactamente lo que hace postMessage con la lista que devuelve toMessage) la misma llamada dio 1 con terminalSorted.length en 0. La causa es que lowerBound sobre un Float64Array desprendido devuelve 0 y la cuenta queda (nPaths − 0)/nPaths. Rompe la regla general del proyecto de no devolver valores callados y mal: lo que se pintaría en pantalla es 'probabilidad 100 %'. El comentario de toMessage advierte que el sim original 'ya no sirve', pero advertir no es lo mismo que fallar ruidosamente. Arreglo de una línea, en las dos copias de la función (la de simulate y la de fromMessage): al entrar, 'if (terminalSorted.length !== nPaths) return null'. Vale la pena también en fromMessage por simetría.
- **[minor]** src/lib/finance/montecarlo.js: el resultado trae percentilesReal y terminalReal, pero contributed y contributedTotal existen solo en nominal, y no hay contributedReal. Quien dibuje la vista real (F4) tiene la trampa servida: la forma obvia, contributed[t] / deflators[t], está mal, porque contributed[t] es una suma de aportaciones hechas en fechas distintas y cada una necesita su propio deflactor. La cuenta correcta sí se puede reconstruir desde afuera (C_t = contributed[t+1] − contributed[t], deflactar cada una con deflators[t] y acumular), pero nadie la va a adivinar y el error resultante es de varios por ciento a 30 años con inflación de 4 %. Arreglo: acumular un segundo arreglo dentro del mismo bucle (una línea: realAcum += cash / deflator antes de escribir contributedReal[t+1]) y exponerlo como contributedReal junto con contributedTotalReal, además de decirlo en las notas para F4.
- **[minor]** src/lib/finance/montecarlo.js: el orden de validación deja pasar callados parámetros rotos cuando el horizonte redondea a menos de un paso. Verificado: simulate({ years: 0, contribution: 100, contributionGrowth: NaN, paths: 1 }) devuelve null, mientras que con years: 1 el mismo objeto lanza 'contributionGrowth tiene que ser un número finito'. Igual con samplePaths, que se valida después del corte. Y years negativos (−5) devuelven null en vez de lanzar, aunque no son 'faltan datos' sino un dato inválido. Es la misma clase de error de orden que el stream ya encontró dos veces (history y maxContribution), nada más que en el tercer sitio. Arreglo: subir finite(contributionGrowth) y finite(samplePaths) arriba, junto al resto de las validaciones, antes de calcular steps; y lanzar cuando years < 0, dejando null solo para el caso legítimo de round(years·stepsPerYear) === 0 con years >= 0.

## A4

Veredicto de la corrida 1: **fail** (5 blocker/major, 2 minor)

- **[blocker]** ledger.js línea 244: la aportación implícita de una compra sin depósito se recalcula contra un efectivo YA negativo, así que refinancia dinero que ya había financiado. `const short = Math.max(0, amount - cash[ccy])`: después de la primera compra sin depósito cash queda en -short, y en la siguiente compra short = amount + |cash|. El error se acumula en cascada.

CÓMO LO VERIFIQUÉ: tres compras de 8,000, 500 y 500 sin ningún depósito. externalFlows devuelve [8000, 8500, 9000] = 25,500 cuando la aportación real es 9,000; fundedCash llega a 16,500 de efectivo fantasma. Corriendo eso por valueSeries: values [8000, 16500, 25500] contra los correctos [8000, 8500, 9000]. moneyWeightedReturn da -0.9996 en vez de ~0. Con precios que sí se mueven, twr reporta 0.051515 cuando la verdad es 0.105914.

POR QUÉ ES BLOQUEANTE: es exactamente la ruta de migración que describe frontend-spec.md (cada posición legada entra como `buy` con date:null y sin depósito si no había posición $MXN). Simulé un portafolio migrado de 4 posiciones con costo real 24,000: valueSeries lo valúa en 58,000, un 141.7 % de sobrevaluación, en el primer render del usuario.

ARREGLO (una línea, verificado): `const short = Math.max(0, amount - (cash[ccy] + funded[ccy]))`. Con eso los flujos del caso de tres compras dan [8000, 500, 500] = 9,000 y fundedCash queda en 0 en los tres cortes; el caso mixto (depósito 3,000 y compras de 8,000 y 500) da depósito 3,000 + funding 5,000 + funding 500. Ninguna prueba actual se rompe: revisé ledger.test.js:139 y performance-ledger.test.js:106/113/149 y todas usan UNA sola compra sin financiar, donde el parche no cambia nada, y la respuesta conocida -0.01 se conserva. Al cerrarlo, agregar al golden de ledger la comparación de externalFlows y de ledgerSnapshots().fundedCash, que hoy no compara y por eso no lo cachó.
- **[major]** performance-ledger.js twrReturns (líneas 198 a 214): el flujo pendiente no se reinicia cuando se re-establece la base después de un corte que no se pudo valuar, así que se cuenta dos veces. En la rama `if (previous === null || current === null) { if (current !== null) previous = current; continue }` la nueva base `current` YA incluye el flujo que entró en ese periodo, pero `pending` lo conserva y se lo vuelve a sumar en el siguiente periodo. La rama `if (!(start > 0))` tiene el mismo problema.

CÓMO LO VERIFIQUÉ: caso de unidad, twrReturns([null, 100, 150], [0, 50, 0]) devuelve [0] cuando debe devolver [0.5]. Y de punta a punta por valueSeries: depósito de 1,000 y compra de 10 X @100 el 2026-01-01 sin precio ese día, depósito de otros 1,000 el 01-02, precios 100 el 02 y 110 el 03. values = [null, 2000, 2100], flows = [1000, 1000, 0]; twr reporta -0.30 cuando el único subperiodo medible es 2100/2000 - 1 = +0.05. Un error de 35 puntos porcentuales.

CUÁNDO SE DISPARA: cualquier usuario cuyo historial de precios no cubra su fecha de movimiento más vieja y que siga aportando, que es lo normal en una cartera migrada. El caso de hueco a media serie (values [100, null, 150]) sí está bien, por eso no lo agarraron las pruebas.

ARREGLO (verificado): poner `pending = 0` junto a `previous = current` en las dos ramas de salto. Lo probé sobre una copia: la respuesta conocida del spec sigue dando -0.009999999999999898, el hueco a media serie sigue dando [0], y los dos casos rotos pasan a [0.05] y [0.5].
- **[major]** src/lib/finance/ledger.js: una venta registrada con una moneda distinta a la del lote se acepta sin avisar y el dinero se abona a la moneda EQUIVOCADA. En runLedger línea 283, `if (proceeds !== null) cash[lot.currency] += proceeds`, usa la moneda del lote y no la del movimiento.

CÓMO LO VERIFIQUÉ: compra de 10 AAPL @150 en USD y venta de 10 @180 capturada por error como MXN. cashBalances devuelve {MXN: 0, USD: 300}: los 1,800 pesos de la venta entraron como 1,800 dólares, un error de ~17x en el saldo. validateTransaction sobre esa venta devuelve {ok: true, errors: []}, o sea que la compuerta que existe para esto la deja pasar.

POR QUÉ IMPORTA: el selector de moneda es por movimiento en el esquema de Transaction, así que equivocarse es un error de captura de un clic, y la regla general del spec es que nunca se mezclan monedas. Hoy se mezclan en silencio y el saldo queda irrecuperablemente mal.

ARREGLO: en validateTransaction, para `sell` y `split`, comparar tx.currency contra la moneda de la posición abierta (derivePositions ya devuelve `currency`, y la función ya recibe `existing`) y agregar un error en español del estilo de los que ya tiene, por ejemplo "AAPL lo tienes en USD, así que la venta también va en USD.". Además, no abonar a ciegas a lot.currency: o se rechaza el movimiento antes de aplicarlo, o se abona a currencyOf(tx.currency), que es el dinero que de verdad entró.
- **[major]** src/lib/finance/ledger.js: derivePositions entrega 5 llaves cuando finance-spec.md sección A4 pide 8. El spec dice literalmente `derivePositions(transactions, {asOf}) -> [{ symbol, quantity, avgCost, currency, costBasis, realizedPnl, firstBuyDate|null, avgFx|null }]`, y lo entregado son solo las cinco primeras; realizedPnl, firstBuyDate y avgFx viven en derivePositionsDetailed.

CÓMO LO VERIFIQUÉ: confirmé las dos afirmaciones del stream. src/lib/portfolio/ledger.contract.test.js usa toEqual contra objetos de exactamente cinco llaves en las líneas 18 y 58, y toEqual falla con cualquier llave de más que traiga valor; ese archivo NO está en los globs de A4 (revisé scripts/ownership.json, sus 21 entradas). Corrí la prueba de contrato: 11 passed. O sea que el bloqueo es real y el rodeo es legítimo.

POR QUÉ SIGUE SIENDO DEFECTO Y NO SOLO UNA NOTA: cualquier F-stream que lea el spec va a llamar derivePositions esperando realizedPnl y va a recibir undefined sin que nada truene, ni en tsc ni en pruebas. Y hay un segundo consumidor vivo que nadie mencionó: src/lib/portfolio/usePortfolios.js línea 11 ya importa derivePositions.

ARREGLO (el que propone el propio stream en docs/requests/A4.md punto 1, y coincido): cambiar esas 2 aserciones de ledger.contract.test.js de toEqual a toMatchObject, mover las tres llaves a derivePositions y dejar derivePositionsDetailed como alias. Son dos líneas en un archivo de S2. Hay que decidirlo en el merge, no dejarlo abierto: si se queda como está, que quede escrito en fase2-A4.md que el spec A4 está desviado a propósito.
- **[major]** src/lib/finance/rebalance.js: el paso codicioso falla mucho más seguido de lo que dice el handoff, que lo presenta como "un caso medido" guardado en el golden. Medí 10.2 % de los casos, no uno.

CÓMO LO VERIFIQUÉ: fuzz de 1,500 carteras aleatorias de 2 activos (precios 10 a 2,010, tenencias 0 a 15, objetivos normalizados, efectivo 0 a 20,000) comparando contra búsqueda exhaustiva de todas las combinaciones enteras que caben en el valor total. En 153 de 1,500 (10.2 %) el plan no alcanza el óptimo entero; la peor brecha de desviación fue 0.270526.

EL PEOR CASO CONCRETO: tenencias {S0: 1, S1: 10}, precios {S0: 643, S1: 65}, objetivos {S0: .7432, S1: .2568}, efectivo 347. El plan devuelve {S0: 1, S1: 6} con desviación 0.370122 y deja 607 de efectivo, o sea 37 % del portafolio SIN INVERTIR después de un rebalanceo. El óptimo entero es {S0: 2, S1: 5} con desviación 0.099596. Pasa porque el piso baja S0 de 1.895 a 1, el codicioso ya no alcanza los 643 de otra S0, y nunca considera vender una S1 para financiar esa compra.

MATIZ JUSTO: el método es el que fija el spec ("floor to whole shares, then greedily buy..."), así que la implementación es fiel. Lo que está mal es la magnitud reportada y que el límite se presente como un caso de laboratorio.

ARREGLO: después del ciclo codicioso, agregar una pasada de mejora local sobre pares (comprar 1 de X financiando con vender 1 de Y) mientras la desviación baje de verdad y el efectivo lo permita; es O(n²) por paso con n ≤ 40 y no cambia las respuestas conocidas. Si se prefiere no tocar el método, entonces corregir el handoff y el encabezado del módulo para decir que en cerca de 1 de cada 10 rebalanceos el plan puede dejar una parte grande en efectivo, y que F1 debe mostrar el efectivo sobrante junto al plan.
- **[minor]** src/lib/finance/rebalance.js línea 120: `deviation` NO incluye el peso del efectivo, aunque el encabezado del módulo (línea 9) y el handoff dicen los dos que sí ("el efectivo cuenta, su objetivo implícito es cero"). deviationOf solo recorre `tradable`.

CÓMO LO VERIFIQUÉ: con targets {} y tenencias {A: 10} a precio 100, el plan vende las 10 A y devuelve deviation.after = 0.000000 con 1,000 de efectivo, o sea 100 % de la cartera fuera de mercado y desviación cero. Algebraicamente la desviación reportada siempre es menor o igual a la que incluye el efectivo, y la diferencia es justo el peso del efectivo sobrante.

POR QUÉ importa poco pero importa: F1 va a pintar ese número como "qué tan lejos quedaste de tu objetivo" y va a mentir por abajo justo cuando queda efectivo sin invertir, que es el caso del hallazgo anterior.

ARREGLO: decidir una de las dos y dejar código y documentación de acuerdo. Si se incluye el efectivo, sumar `Math.abs(left / value)` en deviationOf; ojo que si se incluye también en la función que guía al codicioso, todo movimiento asequible pasa a mejorar y el algoritmo gastaría siempre el efectivo, que es un cambio de método. Lo mínimo es corregir el comentario de la línea 9 y la frase del handoff.
- **[minor]** src/lib/finance/xirr.js: con varios cambios de signo el resultado depende del punto de partida, y el handoff afirma lo contrario ("El orden de entrada no importa y el punto de partida tampoco, probado con guess de -0.9 a 50").

CÓMO LO VERIFIQUÉ: flujos -1000 (2020-01-01), +2600 (2021-01-01), -1680 (2022-01-01), que tienen dos raíces reales. guess 0.1 devuelve 0.203752, guess 0.3 devuelve 0.390834, guess 5 devuelve 0.203752. Las dos son raíces legítimas (xnpv da 0 y 1.1e-13). No es un error de cálculo, es la ambigüedad propia de la TIR, y la bisección solo entra cuando Newton falla, así que la convención de "la raíz más baja" no se aplica siempre.

Efecto secundario medido, para que quede dicho: como MAX_RATE es 1e6, un rendimiento anualizado por arriba de eso devuelve null. -1 y +1000 al día siguiente da null. Es defendible que refuse una anualización absurda, pero que quede escrito, porque hoy no lo está.

ARREGLO: no cambiar el algoritmo. Corregir la afirmación del handoff y del JSDoc: decir que con más de un cambio de signo en los flujos puede haber varias tasas que satisfacen la ecuación y que la que se devuelve depende del punto de partida, y que F1 no presente el XIRR como único cuando la serie tiene aportaciones y retiros alternados. Opcionalmente, devolver también cuántos cambios de signo hubo para que la UI pueda avisar.

Veredicto de la corrida 2: **fail** (4 blocker/major, 6 minor)

- **[blocker]** ledger.js línea 244: el faltante de efectivo de una compra se mide contra `cash[ccy]`, que ya viene negativo de las compras anteriores, así que cada compra sin depósito vuelve a contar el faltante acumulado. `const short = Math.max(0, amount - cash[ccy])`. VERIFICADO con node: con dos compras de 100 y 50 sin depósito, externalFlows da [100, 150] = 250 cuando lo aportado fueron 150, cash queda en -150 y funded en 250, o sea fundedCash = 100 de efectivo fantasma. Con tres compras de 1,000 (que es EXACTAMENTE la forma de un portafolio migrado de v1: el frontend-spec convierte cada posición legacy en un `buy` y solo la posición $MXN en un `deposit`), valueSeries reporta values [6000] con cash [3000] y flows [6000] cuando el valor real es 3000. O sea que a cualquier usuario migrado la app le enseña el DOBLE de su portafolio. Y sí corrompe el TWR: con precios que se mueven, el caso medido da twr = 0.04 cuando el correcto es 0.10. No hay prueba que lo agarre porque los tres casos de funding del stream usan una sola compra sin depósito. ARREGLO (una línea, ya comprobado en copia): `const short = Math.max(0, amount - (cash[ccy] + funded[ccy]))`. Con eso los flujos salen [100, 50], la serie migrada vale 3000 con cash 0, y el caso conocido del spec sigue dando -0.01 sin tocar nada más. Agregar la prueba que falta: dos o más compras sin depósito previo.
- **[major]** performance-ledger.js, tiempo del flujo en twrReturns: `flows[i]` incluye los movimientos fechados EN `dates[i]` (el snapshot se toma después de aplicar la transacción de ese día), así que ese dinero ya está dentro de `values[i]`, pero la fórmula lo mete en la base del periodo con `values[i-1] + flows[i]`, o sea le regala un periodo completo de exposición que no tuvo. VERIFICADO: depósito de 50 el mismo día en que la posición pasó de 100 a 110 da twr = 0.06666666666666665 cuando el rendimiento real del periodo es 0.10. En una serie diaria TODO depósito cae en una fecha de valuación, así que no es un caso raro: es el caso normal, y castiga el rendimiento cada vez que alguien aporta. El caso conocido del spec no lo destapa porque ahí el precio no se mueve el día del depósito (160 = 110 + 50), así que las dos convenciones dan 0. ARREGLO (comprobado en copia, respeta todas las respuestas conocidas): usar la convención de fin de periodo, que es la coherente con cómo el módulo arma sus propios cubos de flujo: `out.push((current - pending) / previous - 1)` y cambiar la guarda `if (!(start > 0))` por `if (!(previous > 0))`. Re-corrido: spec -0.01, retiro 0.21, sin flujos 0.2, flujo del primer corte 0.1, y el caso roto pasa a 0.10. Queda pendiente documentar que con series semanales o mensuales el flujo a media semana no queda bien con ninguna de las dos convenciones puras (ahí tocaría Dietz modificado).
- **[major]** ledger.js mezcla monedas dentro de un mismo símbolo y validateTransaction lo deja pasar, contra la regla general del spec ('Never mix currencies'). El lote guarda la moneda de la PRIMERA compra y las siguientes suman su costo sin convertir. VERIFICADO: comprar 10 AAPL a 200 USD y luego 10 AAPL a 3,600 MXN devuelve UNA posición {quantity: 20, avgCost: 1900, currency: 'USD', costBasis: 38000}, que es basura, y validateTransaction(la segunda compra, [la primera]) responde {ok: true, errors: []}. No es hipotético: AAPL cotiza en el SIC en pesos y en Nasdaq en dólares, y el usuario puede capturar una de cada una sin darse cuenta. ARREGLO: en validateTransaction, comparar tx.currency contra la moneda de la posición abierta de ese símbolo y devolver un error en es-MX ('Ya tienes AAPL en USD. Captura este movimiento en la misma moneda o usa el símbolo de la otra bolsa.'); y en runLedger, cuando la moneda del movimiento no coincide con lot.currency, poner lot.cost = null en vez de sumar, para que avgCost salga null ('s/d') y no un número inventado.
- **[major]** rebalance.js emite cantidades fraccionarias cuando la tenencia es fraccionaria, justo debajo de su propia nota que promete lo contrario ('Las cantidades van en acciones o títulos enteros, sin fracciones.'). VERIFICADO: wholeShareRebalance({holdings:{A:10.5}, prices:{A:100}, targets:{A:1}, cash:0}) devuelve trades [{symbol:'A', side:'venta', quantity:0.5, amount:50, price:100}]. Pasa porque la base es Math.floor(target*value/price) y el delta se saca contra un current fraccionario. Las tenencias fraccionarias son reales en el producto: la migración v1 lee `shares` sin redondear y varios intermediarios venden fracciones. La UI de F1 acabaría diciendo 'vender 0.5 títulos' arriba de la nota que dice que no hay fracciones. ARREGLO: antes del paso codicioso, pegar el delta a entero dejando la cola fraccionaria quieta, `qty[s] = current[s] + Math.trunc(qty[s] - current[s])`, y agregar la prueba con tenencia fraccionaria que hoy no existe.
- **[minor]** derivePositions devuelve cinco llaves y el spec (sección A4) pide ocho: faltan realizedPnl, firstBuyDate y avgFx, que viven en derivePositionsDetailed. CONFIRMADO leyendo src/lib/portfolio/ledger.contract.test.js: sí usa toEqual contra objetos de exactamente cinco llaves en 2 de sus 11 casos, y ese archivo no está en los globs de A4, así que la desviación está justificada y documentada en docs/requests/A4.md. No es culpa del stream, pero sigue siendo una desviación del spec que hay que cerrar antes de que F1 llame a la función equivocada y pinte 's/d' donde hay dato. ARREGLO (dos líneas, del dueño de ese archivo): cambiar esas dos aserciones a toMatchObject, mover las tres llaves a derivePositions y dejar derivePositionsDetailed como alias.
- **[minor]** performance-ledger.js convierte los flujos externos con lastKnown(fx, fecha del flujo) y, si ahí no hay nada, con el tipo de cambio de la fecha del corte, y nunca lee el `fxRate` del movimiento, que el esquema de Transaction guarda precisamente como 'MXN per USD at trade date'. VERIFICADO: depósito de USD 1,000 el 2026-01-01 con fxRate 18, fxByDate solo {'2026-01-02': 18.5}, y el flujo se contabiliza en 18,500 en vez de 18,000, con `missing` VACÍO, o sea sustitución silenciosa de fuente. Segunda cara del mismo problema, también verificada: cuando la conversión de plano falla, el flujo se trunca en silencio (depósito de USD 1,000 más MXN 500 sin fxByDate da flows [500], sin marca en `flows`). ARREGLO: cargar `fxRate` en el typedef ExternalFlow, preferirlo sobre fxByDate cuando venga, y marcar en `missing` el flujo que no se pudo convertir en vez de sumar solo la parte que sí.
- **[minor]** Vender más de lo que se tiene se recorta en silencio en todos los caminos de derivación, sin marca en la salida. VERIFICADO: comprar 10 y vender 25 devuelve una venta de 10 con proceeds 1300 y cash 300, sin nota ni campo `dropped`. validateTransaction sí lo rechaza con el mensaje correcto ('No puedes vender 25 de A: a esa fecha tienes 10.'), así que la captura está cubierta, pero importJSON y los datos migrados no pasan por ahí. ARREGLO: que derivePositions/realizedSales expongan un canal de avisos como ya hace tax-mx con `dropped` y `notes`, para que F1 pueda decir 'se recortó una venta'.
- **[minor]** tax-mx.js exporta LOSS_CARRY_YEARS = 10 y lo cita en una nota visible, pero nunca lo aplica: el carry se arrastra sin caducar. VERIFICADO leyendo el bucle por ejercicio, que solo suma y resta sin mirar la distancia entre años. Una pérdida de 2010 seguiría restando a una ganancia de 2026. El resto de la regla fiscal sí quedó bien y lo comprobé corriéndolo: el factor usa INPC(mes anterior a la venta)/INPC(mes de la compra) (compra 2024-03, venta 2025-07 toma 2025-06/2024-03 = 1.05), la venta del mismo mes no actualiza, las pérdidas restan dentro del mismo ejercicio (500 de ganancia contra 700 de pérdida da 0 de impuesto y 200 de carry) y las pérdidas de un año posterior NO borran el impuesto ya causado del anterior. ARREGLO: guardar el carry por ejercicio de origen y dejarlo caducar a los diez, o quitar la constante de la nota mientras no se aplique.
- **[minor]** xirr.js: dos comportamientos sin documentar. Uno, devuelve null cuando la raíz real pasa de MAX_RATE = 1e6 (VERIFICADO: -100 y +10,000 a 30 días devuelve null), y el JSDoc atribuye el null a 'no converge', lo cual despista; la respuesta es honesta pero la razón que documenta no es la real. Dos, con varios cambios de signo devuelve la raíz a la que caiga el guess, sin avisar de que hay más de una (VERIFICADO: [-1, +5, -6] anuales devuelve 1.0077656981354841 con el guess por omisión y 1.9728424003568459 con guess 3; comprobé con xnpv que LAS DOS son raíces de verdad, xnpv 0 y -1.1e-16, o sea que el solucionador está bien, lo que falta es decirlo). ARREGLO: documentar el tope de 1e6 como causa aparte del null, y anotar en el JSDoc que con flujos de signo alternado el XIRR puede tener varias soluciones y esta devuelve una.
- **[minor]** rebalance.js: el paso codicioso deja efectivo ocioso grande y no llega al óptimo entero. VERIFICADO corriendo el caso que el propio stream midió y guardó en el golden (`greedy-vs-optimo`): 10 A y 2 B, precios 250 y 1500, objetivos 60/40 y 1,200 de efectivo, el plan vende 1 B, compra 6 A y DEJA los 1,200 completos sin invertir, que son 17.9 % del portafolio, con desviación .179104; la mejor combinación entera es 14 A y 2 B con .125373. Lo anoto porque un plan de rebalanceo que deja casi un quinto en efectivo se ve mal en pantalla, no porque el stream se haya equivocado: implementó el piso más codicioso tal como lo fija el spec, lo midió y lo documentó en el encabezado del módulo, en el golden y en sus open issues. La decisión es del orquestador, no del stream. ARREGLO si se quiere cerrar: una pasada final de mejora local (para cada par, probar vender 1 de x y comprar 1 de y, y quedarse con el cambio si baja la desviación sin dejar el efectivo negativo), o redondear en vez de pisar del lado de las ventas. Comprobé que el resto del módulo no gasta de más: el efectivo nunca queda negativo en la base (Σ floor(t·V/p)·p ≤ V) ni en el paso codicioso (guarda `p > left + EPS`), allowSell:false solo invierte efectivo, y minTrade no deja el efectivo en negativo al deshacer.

## A5

Veredicto de la corrida 1: **pass_with_issues** (2 blocker/major, 5 minor)

- **[major]** docs/metodologia/fuentes-de-datos.md línea 59 afirma que el tipo de cambio es la ÚNICA excepción a la regla de no rellenar hacia adelante ('La única excepción es el tipo de cambio, que se puede arrastrar hasta 3 días'). El spec define una segunda excepción, mucho más grande: finance-spec.md, A1 rates.js, dice que rfSeriesForDates hace 'forward-fill yields only, max 45 days, else null'. O sea que la tasa libre de riesgo, que entra al Sharpe, al Sortino, al Treynor, al portafolio tangente y al CAPM, se puede arrastrar hasta 45 días y la página que existe justamente para decir qué se arrastra y qué no lo niega. Ni docs/metodologia/riesgo.md ni el término 'tasa-libre-de-riesgo' del glosario lo mencionan tampoco. Cómo lo verifiqué: leí las dos fuentes; el 3 días del FX sí está respaldado (api-v2-spec.md línea 28, 'forward-filled at most 3 days for FX gaps, reported in meta.notes'), el 45 días del rf no aparece en ninguna página de A5. Arreglo: en fuentes-de-datos.md cambiar 'La única excepción' por dos excepciones enumeradas y agregar la del rf con su límite de 45 días y el 'else null'; repetir la línea en la sección 'Sharpe y Sortino' de riesgo.md y en el campo largo del término 'tasa-libre-de-riesgo'.
- **[major]** docs/metodologia/valuacion-dcf.md línea 102, en la guarda del valor terminal, dice: 'Con WACC = 9% y g = 8.9%, el valor terminal es 90 veces el flujo.' El número está mal por un factor de 12. Con la fórmula que la misma página escribe cuatro secciones antes, FCFF_n(1 + g)/(WACC − g) = 1.089/0.001 = 1,089 veces el flujo, no 90. Cómo lo verifiqué: lo calculé en node junto con el resto de los ejemplos (1.089/0.001 = 1089.00); 90 veces correspondería a g ≈ 7.8%, que da 89.83. Duele más porque es el único número de esa página cuyo trabajo es mostrar por qué la guarda existe, y subestima el estallido del valor terminal justo en el caso que se quiere ilustrar. Arreglo: escribir '1,089 veces el flujo', o cambiar el supuesto a g = 7.8% si se prefiere un número redondo. La línea equivalente del glosario (término 'crecimiento-terminal') no tiene este problema: ahí el ejemplo 161.051 / 3% / 9% da 2,764.71 y 1,796.87, que sí cuadran.
- **[minor]** docs/metodologia/riesgo.md línea 49 dice 'Usa una inversa acurada de la normal, la aproximación de Acklam.' 'Acurada' no es español, es un calco directo de 'accurate' (el spec en inglés dice 'an accurate normal inverse CDF (Acklam)'). Estas páginas son copy visible: el spec de A5 dice que las páginas de /aprender las van a renderizar. Además se perdió 'acumulada': Acklam invierte la normal ACUMULADA, no 'la normal'. Cómo lo verifiqué: grep de anglicismos sobre src/content y docs/metodologia; es el único calco real, el resto de los aciertos son títulos de artículos en inglés (legítimos) o comentarios de código en .js que nadie renderiza. Arreglo: 'Usa una inversa precisa de la normal acumulada, la aproximación de Acklam.'
- **[minor]** src/content/glossary.js línea 476, término 'hhi', comoLeer: 'Abajo de 0.10 se considera repartido y arriba de 0.25 concentrado, con los mismos cortes que usa la autoridad de competencia', con fuente que cita a la Comisión Federal de Competencia Económica. Esos cortes no son de COFECE. COFECE trabaja con el IHH en escala de 10,000 puntos y su criterio de no objeción es IHH por debajo de 2,000 puntos, o sea 0.20 en la escala de fracciones que usa Kaizen, con delta menor a 75. El par 0.15 / 0.25 es de las Horizontal Merger Guidelines de Estados Unidos de 2010 y el par 0.10 / 0.18 es de la versión de 1992. El 0.10 / 0.25 que escribe el glosario no es de ninguna de las tres. En un glosario cuyo argumento de venta es 'cada término trae de dónde sale', una atribución equivocada cuesta credibilidad. Arreglo: o quitar la atribución ('cortes de uso común en el análisis de concentración') o usar el número real de COFECE (0.20) y decirlo así.
- **[minor]** El glosario nunca da la tasa de retención de las distribuciones de FIBRAs, y el único término de retención que tiene dice 10 por ciento. Concretamente: 'rendimiento-por-distribucion' (glossary.js línea 1492) dice 'a las distribuciones de FIBRAs se les retiene ISR' sin tasa y tiene a 'retencion-por-dividendos' entre sus relacionados; 'fibra' (línea 1408) dice 'con retención sobre la distribución' sin tasa; y 'retencion-por-dividendos' dice '10 por ciento adicional' del art. 140 y a su vez lista a 'fibra' entre sus relacionados. El 10 por ciento del art. 140 no aplica a FIBRAs: el art. 188 fracción IV de la LISR hace que el fiduciario retenga a la tasa del art. 9 (30 por ciento) sobre el resultado fiscal distribuido, que para persona física es pago provisional. Un lector que llega al rendimiento por distribución de una FIBRA y sigue la liga a 'retención por dividendos' concluye 10 por ciento. No hay una afirmación falsa, es una omisión con liga cruzada que induce al error, y es materia fiscal mexicana para el usuario objetivo. Arreglo: poner la tasa en 'rendimiento-por-distribucion' y en 'fibra' ('retención a la tasa del art. 9, 30 por ciento, como pago provisional para personas físicas'), y agregar una línea en 'retencion-por-dividendos' aclarando que el 10 por ciento del art. 140 no cubre a las FIBRAs. Lo mismo en docs/metodologia/fibras.md línea 99, que hoy solo dice 'según los artículos 187 y 188'.
- **[minor]** docs/metodologia/simulador.md línea 73 afirma como hecho medido algo que nadie pudo medir: 'Rendimiento: 10,000 caminos por 360 pasos corren en menos de 400 ms en node, así que la pantalla no necesita un worker para no trabarse.' Los 400 ms son un OBJETIVO del spec, no un resultado ('Performance: 10k paths × 360 steps < 400 ms in node', finance-spec.md A3), montecarlo.js lo está escribiendo A3 en paralelo y no existe en este worktree, y PLAN.md línea 316 tiene al Web Worker como el punto 1 de la lista de 'cut first'. O sea que la página cierra una decisión de producto a partir de una medición que no ocurrió. Cómo lo verifiqué: busqué montecarlo.js en el worktree (no está) y leí las dos líneas del spec y del PLAN. Arreglo: 'El objetivo de rendimiento es que 10,000 caminos por 360 pasos corran en menos de 400 ms en node' y quitar la conclusión sobre el worker, o dejarla condicionada a que se cumpla la medición.
- **[minor]** docs/metodologia/backtest.md línea 51, en la tabla de métricas, escribe la caída máxima como 'Mínimo de V_t / máximo hasta t − 1'. Leído tal cual, 'máximo hasta t − 1' es el máximo sobre los periodos 0..t−1, que excluye el punto actual y no es la definición: el −1 es la resta de la unidad, no parte del subíndice. Las otras dos veces que el mismo equipo escribe esta fórmula lo hace bien y sin ambigüedad (glossary.js, término 'drawdown-maximo': 'DD_t = V_t / max(V_0..V_t) − 1'; riesgo.md: 'el valor entre el máximo alcanzado hasta esa fecha, menos uno'). Con la lectura equivocada, una serie en máximo nuevo daría drawdown positivo y el caso conocido de −33.3333 por ciento no reproduce. Cómo lo verifiqué: comparé las tres redacciones contra el caso conocido del spec (100, 120, 90, 110, 80, 130 con pico 1, fondo 4, recuperación 5). Arreglo: escribir 'mínimo de (V_t / máx(V_0..V_t)) − 1' en la tabla.

Veredicto de la corrida 2: **pass_with_issues** (0 blocker/major, 7 minor)

- **[minor]** docs/metodologia/valuacion-dcf.md:102 tiene una cifra mal calculada por un factor de 12. Dice: 'Con WACC = 9% y g = 8.9%, el valor terminal es 90 veces el flujo'. Con la misma fórmula que la página define arriba, FCFF_n(1+g)/(WACC−g) = 1.089/0.001 = 1,089 veces el flujo (1,000 si se omite el (1+g)). Verificado con node: (1*(1+0.089))/(0.09-0.089) = 1088.999999999999. El ejemplo existe justo para ilustrar por qué la guarda WACC−g ≥ 2 pp es necesaria, así que el número equivocado debilita el argumento que quiere hacer. Nota de contexto: el stream auditó los 91 ejemplos numéricos del glosario, pero no los de las páginas de metodología, y este es el único que encontré fuera. Arreglo: cambiar '90 veces el flujo' por '1,089 veces el flujo', o dejar el 90 y corregir el supuesto a g = 7.8 por ciento, que es el par que sí da 90.
- **[minor]** docs/metodologia/fuentes-de-datos.md:58-60 afirma una regla de procedencia más estricta que la que el spec implementa. Dice: 'No se rellena hacia adelante... La única excepción es el tipo de cambio, que se puede arrastrar hasta 3 días'. El arrastre de 3 días del FX sí viene de api-v2-spec.md línea 28, pero finance-spec.md línea 36 documenta una segunda excepción: rfSeriesForDates hace forward fill de los rendimientos de la tasa libre de riesgo hasta 45 días ('forward-fill yields only, max 45 days, else null'). Esa serie alimenta Sharpe, Sortino, Treynor y todas las regresiones en exceso, o sea casi toda la pantalla de riesgo, y ninguna página de A5 la menciona (grep de '45' y 'arrastr' en docs/metodologia: solo aparece el FX). En una app cuyo argumento es ser honesta sobre la procedencia, decirle al usuario que nada se rellena cuando la tasa sí se rellena es un hueco real. Arreglo: agregar la excepción en fuentes-de-datos.md y en la sección de Sharpe y Sortino de riesgo.md: 'la tasa libre de riesgo se arrastra hasta 45 días; si no hay dato en esa ventana, el periodo sale nulo y la pantalla muestra s/d'.
- **[minor]** src/content/glossary.js:796, término isr-ganancia-de-capital: el párrafo largo se contradice con su propia fórmula, con su propio ejemplo y con el spec. Dice que Kaizen 'no toma en cuenta tu situación completa, otras operaciones fuera de la plataforma ni los detalles de actualización por inflación', pero el campo formula del mismo término dice 'costo actualizado', el campo ejemplo aplica un factor de 1.05, docs/metodologia/portafolio.md describe el paso 2 como actualizar por INPC(mes anterior a la venta)/INPC(mes de compra), y finance-spec A4 define isrOnGains con esa misma actualización. Un usuario que lea el término va a creer que la estimación ignora la inflación cuando sí la incorpora. Arreglo: cambiar esa cláusula por lo que de verdad queda fuera, por ejemplo 'no conoce tus operaciones fuera de la plataforma ni tu situación fiscal completa, y cuando falta el INPC de un mes usa el costo sin actualizar y lo dice'.
- **[minor]** docs/metodologia/riesgo.md:48-49 usa una palabra que no existe en español: 'Usa una inversa acurada de la normal, la aproximación de Acklam'. Es calco literal del 'accurate normal inverse CDF' del finance-spec. Verificado con grep: es la única ocurrencia de spanglish que encontré en las 11 páginas y en los 95 términos (el resto de los anglicismos son nombres de métrica que el propio spec usa en inglés: tracking error, information ratio, earnings yield, cap rate, drawdown, walk forward). Estas páginas las va a renderizar la sección Aprender, así que es copy visible. Arreglo: 'una inversa precisa de la normal acumulada, la aproximación de Acklam'.
- **[minor]** src/content/glossary.js:269 y 285, términos var y cvar: los ejemplos no se reproducen con el sigma que ellos mismos citan. Dicen 'con μ = 4.5 por ciento y σ = 5.92 por ciento, el paramétrico da 5.23 por ciento' y '...el CVaR paramétrico al 95 por ciento es 7.70 por ciento'. Con σ = 5.92 por ciento exacto sale VaR 5.2375 (5.24) y CVaR 7.711 (7.71); verificado con node usando z = 1.6448536269514722. Los 5.23 y 7.70 del spec solo salen con σ = 5.9161 por ciento, que es la desviación muestral con n−1 de los 20 rendimientos, y así está bien escrito en docs/metodologia/riesgo.md ('desviación de 5.91608 por ciento'). Quien recalcule desde el glosario va a pensar que la biblioteca está mal. Arreglo: escribir σ = 5.9161 por ciento en los dos ejemplos del glosario, igual que en riesgo.md.
- **[minor]** docs/metodologia/simulador.md:73 presenta como hecho medido lo que en el spec es un requisito, y saca de ahí una conclusión de diseño que el spec deja abierta: '10,000 caminos por 360 pasos corren en menos de 400 ms en node, así que la pantalla no necesita un worker'. finance-spec A3 lo escribe como objetivo ('Performance: 10k paths × 360 steps < 400 ms in node') y en el encabezado de A3 lista '(+ optional montecarlo.worker.js)'. Además src/lib/finance de este worktree solo tiene ledger.js: montecarlo.js todavía no existe, así que nadie ha medido nada. Si A3 no llega a los 400 ms, la página de metodología queda afirmando algo falso y descartando el worker que el spec sí contempla. Arreglo: 'el objetivo es que 10,000 caminos por 360 pasos corran en menos de 400 ms en node; si no se alcanza, la simulación se mueve a un worker para no trabar la pantalla'.
- **[minor]** src/content/glossary.js:476, término hhi: 'Abajo de 0.10 se considera repartido y arriba de 0.25 concentrado, con los mismos cortes que usa la autoridad de competencia'. Ese par de cortes no corresponde a ninguna autoridad en particular: las guías estadounidenses de 2010 usan 1,500 y 2,500 puntos (0.15 y 0.25), las anteriores a 2010 usaban 1,000 y 1,800 (0.10 y 0.18), y la COFECE trabaja con un umbral de 2,000 puntos más el delta de la operación. La cifra mezcla dos regímenes y les cuelga una atribución que ninguno respalda. Es el único término donde la fuente no aguanta el dato, en un archivo que por lo demás cita bien. Arreglo: quitar la atribución o precisarla, por ejemplo 'cortes de referencia del análisis de competencia: por debajo de 1,500 puntos se considera repartido y por arriba de 2,500 concentrado, en las guías estadounidenses de 2010'.

## B1

Veredicto de la corrida 1: **pass_with_issues** (2 blocker/major, 3 minor)

- **[major]** Bloqueo de cuenta ajena en /auth/login, justo lo que el punto 1 del encargo pedía eliminar. En kaizen_api/security/ratelimit.py, LoginRateLimiter.check() gasta la ficha de la cubeta por usuario ANTES de verificar la contraseña, y routers/auth.py llama a refund_user() después de pasar esa compuerta. Con la cubeta vacía la petición se rechaza sin llegar nunca a authenticate(), así que la contraseña correcta también se rechaza. Verificado en el servidor vivo (proceso limpio, puerto 8158): login correcto de 'ana' 200, luego 10 contraseñas malas desde 10 X-Forwarded-For distintas (401 x10), y enseguida 'ana' con su contraseña buena devuelve 429 RATE_LIMITED con retry-after: 360; reproducido idéntico con TestClient. Como la cubeta se rellena a 1 ficha cada 360 s, una contraseña mala cada 6 minutos mantiene a esa persona fuera indefinidamente, a un costo de 10 peticiones por hora. La suite no lo detecta porque test_failed_attempts_still_fill_the_hourly_bucket prueba el intento 11 con contraseña MALA, nunca con la buena. Arreglo: separar la compuerta. Antes de autenticar se revisa solo la cubeta por IP; se verifica la credencial; y la ficha por usuario se gasta SOLO cuando authenticate() devolvió None, devolviendo 429 en ese caso. Para que el techo de fuerza bruta siga sirviendo sin volver a abrir el bloqueo, la cubeta de fallas se lleva por (usuario, IP) en vez de por usuario, de modo que la cubeta agotada frena a la IP atacante y nunca a la legítima, y si se quiere un tope global por usuario se deja uno mucho más ancho. Agregar la prueba que falta: agotar la cubeta con contraseñas malas y exigir que la contraseña correcta siga dando 200.
- **[major]** El límite por IP del login se esquiva por completo mandando X-Forwarded-For como cabecera repetida. En kaizen_api/security/ratelimit.py, client_ip() hace headers.get("x-forwarded-for"), y cuando la petición trae varias cabeceras con ese nombre Starlette devuelve solo la PRIMERA, que es la que escribió el cliente. El atacante manda su propia X-Forwarded-For delante de la que agrega el proxy y vuelve a controlar la llave de la cubeta, rotándola en cada intento. Verificado contra el servidor uvicorn vivo en 8158, usando un usuario distinto por petición para aislar la cubeta por IP: con UNA cabecera y cadena forjada salen 401 x5 y 429 desde la sexta, o sea que el límite sí funciona; con DOS cabeceras salen 401 x8 y nunca aparece el 429. El propio ProxyHeadersMiddleware de uvicorn ya resuelve esto uniendo todos los valores (b", ".join(x_forwarded_for_values) en uvicorn/middleware/proxy_headers.py), así que el arreglo es la misma idea y cabe en un archivo de B1: values = headers.getlist("x-forwarded-for") if (headers is not None and hops) else []; forwarded = ", ".join(v for v in values if v), con un getattr de respaldo para mapas simples que no tengan getlist. Agregar la prueba con dos cabeceras, que hoy no existe (tests/unit/b1/test_login_rate_limit.py arma los casos con Headers({"x-forwarded-for": value}), una sola).
- **[minor]** El 503 de la guarda de carga contesta con código RATE_LIMITED, pero el OpenAPI anuncia que el 503 significa UPSTREAM_UNAVAILABLE o NOT_CONFIGURED. ERROR_RESPONSES[503] en kaizen_api/http_responses.py, que es archivo de B1, describe el 503 como 'Fuente caída o sin configurar (UPSTREAM_UNAVAILABLE, NOT_CONFIGURED)', mientras ConcurrencyLimitMiddleware devuelve 503 con code RATE_LIMITED. Verificado en la sonda de carga: cuerpo {"error":{"code":"RATE_LIMITED",...}} con status 503. Un cliente que ramifique por código en un 503 se topa con uno que el contrato no anuncia. Arreglo de una línea en el archivo que B1 ya posee: sumar RATE_LIMITED a la descripción del 503, por ejemplo 'Fuente caída o sin configurar (UPSTREAM_UNAVAILABLE, NOT_CONFIGURED) o servidor saturado (RATE_LIMITED, con Retry-After)'.
- **[minor]** La cubeta por IP del login cuenta también los ingresos correctos, así que una oficina detrás de NAT se bloquea sola. Verificado: 8 empleados distintos, cada uno con su contraseña buena, desde la misma X-Forwarded-For dan [200, 200, 200, 200, 200, 429, 429, 429]. El 5 por minuto es el número de la spec, así que no es incumplimiento, pero el producto apunta a programas de bienestar financiero de empleadores, que es justo el caso donde mucha gente comparte IP de salida, y a las 9 de la mañana eso se nota. La misma idea del refund que se aplicó a la cubeta por usuario sirve aquí: devolver la ficha por IP cuando el login resultó correcto, dejando el límite como tope de intentos fallidos. Conviene que lo decida el dueño antes de cambiar un número de la spec.
- **[minor]** En producción la regex CORS por omisión acepta cualquier proyecto de Vercel cuyo nombre empiece con 'newkaizen-', incluido uno que registre un tercero. Verificado: con el Origin https://newkaizen-evil-attacker.vercel.app la respuesta trae access-control-allow-origin con ese mismo origen (los señuelos https://newkaizen.vercel.app.evil.com y https://evil.com sí se rechazan). El daño está acotado porque allow_credentials va en false y el token viaja en el header, no en cookie, y B1 agregó VERCEL_TEAM_SLUG más un aviso al arrancar. Aun así el default inseguro sigue siendo el default. Arreglo en kaizen_api/settings.py, que es de B1: en producción convertir ese aviso en SettingsError salvo que venga VERCEL_TEAM_SLUG o ALLOWED_ORIGIN_REGEX explícita, o estrechar DEFAULT_ORIGIN_REGEX al origen exacto de producción y dejar los previews solo para desarrollo.

## B2a

Veredicto de la corrida 1: **pass_with_issues** (3 blocker/major, 3 minor)

- **[major]** /v2/history publica como observaciones reales los precios que Yahoo rellena hacia adelante en días que la BMV estuvo cerrada, sin nota ni marca. Verificado con datos: en la serie de 1 año de NAFTRAC.MX el cierre del 2026-09-16 (Independencia) es 63.2599983215332, idéntico bit a bit al del 2026-09-15; lo mismo pasa el 2026-05-01 contra el 04-30 (66.66264343261719), el 2026-02-02 contra el 01-30 (66.08116912841797), el 2026-03-16 contra el 13 (64.32122802734375) y el 2026-04-02 contra el 01 (68.1099624633789). Las cinco fechas están en kaizen_api/data/holidays_bmv.json como días inhábiles. WALMEX.MX no las trae, así que el relleno depende de cómo sirva Yahoo cada símbolo (el fixture de NAFTRAC viene con índice datetime64[s, America/New_York] y su conjunto de días es exactamente el calendario de la NYSE). El efecto financiero es concreto: cinco rendimientos diarios de 0.00% inventados por año, que bajan la volatilidad medida y sesgan beta y Sharpe, que es justo lo que este rediseño quiere arreglar. El spec pide fechas REALES en /v2/history y prohíbe rellenar precios en /v2/panel; un cierre repetido en día cerrado es un relleno, solo que hecho río arriba. Arreglo concreto: en kaizen_api/domain/history.py::get_series, cuando interval == '1d' y exchange_for(symbol) devuelve una bolsa, descartar toda barra cuya fecha no sea jornada de ese calendario (market_calendar.session(load_calendar(ex), fecha) is None) y agregar una nota que diga cuántas se quitaron y por qué. Reusa maquinaria que B2a ya tiene y es un puñado de líneas; para símbolos de EE. UU. queda en nada, porque AAPL no trae barras el 7 de septiembre ni el 3 de abril.
- **[major]** La costura CONGELADA domain.fx.convert(serie, ...) tira en silencio las primeras observaciones cuya fecha no tiene barra de tipo de cambio, en vez de rellenarlas como dice su propio contrato. La causa está en kaizen_api/domain/fx.py::_convert_series: pide daily_range(first, last), o sea recorta el FX al mismo rango de la serie, así que rate_on() no tiene hacia dónde mirar atrás en la PRIMERA fecha. Verificado con una prueba que escribí: una Series de 3 puntos con índice 2026-01-01, 2026-01-02 y 2026-01-05 sale con 2 puntos; el tipo de cambio del 2025-12-31 (17.97879981994629) existe y get_series sí lo usa (rate_on lo encuentra arrastrado 1 día). El docstring del módulo dice 'rellenando hacia adelante a lo más 3 días y anotando cada relleno' y ninguna de las dos cosas pasa por este camino. Importa porque es la costura que el propio handoff ofrece a B3a (beta) y B3c (precios de FIBRAs): una serie que empieza en lunes festivo pierde su primer punto sin avisar. Arreglo concreto: en _convert_series pedir daily_range(first - timedelta(days=MAX_FORWARD_FILL_DAYS + 7), last), que es exactamente el margen que _rate_for_date ya usa para el caso escalar, y devolver o registrar cuántos puntos se arrastraron. Conviene revisar por lo mismo que series_for() pida un margen antes del inicio del periodo, para que get_series no quede expuesto al mismo borde cuando la primera barra de precio caiga antes de la primera barra de FX.
- **[major]** Ni /v2/quotes ni /v2/history normalizan GBp ni ZAc, aunque el contrato congelado lo exige por escrito. kaizen_api/schemas.py línea 62 dice: 'Yahoo reporta unidades menores en algunas plazas (GBp en Londres, ZAc en Johannesburgo): el proveedor las normaliza a GBP y ZAR dividiendo el monto entre 100 antes de armar la respuesta'. Ese proveedor es kaizen_api/providers/yahoo/prices.py, que es de B2a, y no hay una sola mención de GBp o ZAc en todo el paquete fuera de ese docstring. Verificado con una prueba: quotes_router._quote('VOD.L', {'currency': 'GBp', 'regularMarketPrice': 7420.0, ...}) devuelve {'price': 7420.0, 'currency': 'GBP'}, o sea 7420 peniques etiquetados como 7420 libras, cien veces el valor real; history_domain.native_currency hace lo mismo porque su línea `reported = info.currency.strip().upper()` convierte 'GBp' en 'GBP' sin tocar el monto, y luego pasa el filtro `len == 3 and isalpha`. El daño es un precio cien veces mayor con un código ISO que se ve perfectamente correcto, que es la peor combinación posible para una app cuyo argumento es la honestidad del dato. Arreglo concreto: en providers/yahoo/prices.py agregar un paso de normalización en la frontera, por ejemplo MINOR_UNITS = {'GBP': ('GBP', 100.0), 'ZAC': ('ZAR', 100.0)} comparando contra el valor crudo antes de .upper(), aplicar el divisor a price, previousClose y a los cierres de _dates_and_closes, y que native_currency devuelva ya el código mayor. Alcance real: solo muerde símbolos .L y .JO, que no son el camino principal de esta app, por eso no lo pongo como bloqueante.
- **[minor]** /v2/fx/history devuelve dos valores distintos para la MISMA fecha según el parámetro start, porque el periodo de Yahoo se elige con _period_for_span y cada ventana es una llamada distinta. Verificado en vivo el 2026-09-22: con start=2026-08-25 el valor de ese día es 17.28830909729004 y con start=2026-03-01 o start=2025-09-20 es 17.297130584716797; /v2/fx spot coincide solo con el segundo. Las fechas ya cerradas sí coinciden exactas (17.227500915527344 el 21 de septiembre en las tres ventanas), así que el problema es únicamente la barra de HOY, que todavía se está formando. Se arrastra a /v2/history: el mismo símbolo con ccy=MXN da un precio distinto para hoy según pidas range=1mo o range=1y. Encima sale con meta.stale=false, asOf con fecha sin hora y ninguna nota que diga que el último punto es una barra intradía en formación. El stream lo declaró como limitación conocida, pero tiene arreglo barato: compartir una sola serie de FX cacheada entre /v2/fx, /v2/fx/history y la conversión de históricos, y agregar a meta.notes que el último punto todavía se mueve mientras el mercado esté abierto.
- **[minor]** Un rango de fechas enteramente en el futuro en /v2/fx/history contesta 503 UPSTREAM_UNAVAILABLE, que culpa al proveedor de un error de la solicitud. Verificado: GET /v2/fx/history?start=2030-01-01&end=2030-12-31 devuelve {'error': {'code': 'UPSTREAM_UNAVAILABLE', 'message': 'No pudimos obtener el tipo de cambio del dólar. Intenta más tarde.'}}. El camino es kaizen_api/domain/fx.py::daily_range, donde `inside` queda vacío y se levanta no_fx(). Además le dice al usuario 'intenta más tarde', que nunca va a funcionar. Arreglo concreto: en check_date_range o al entrar a daily_range, si start es posterior a hoy, responder 422 VALIDATION_ERROR con un mensaje que diga que la fecha inicial todavía no ocurre, y dejar el 503 para cuando de verdad falló la fuente.
- **[minor]** La regla nueva de frescura no aplica calendario a los símbolos de EE. UU. con guion o punto. En kaizen_api/domain/history.py::exchange_for, la última línea es `return 'nyse' if up.isalpha() else None`, así que BRK-B, BF-B o cualquier ticker con clase de acción cae a la tolerancia genérica de 4 días naturales en vez de compararse contra la última jornada cerrada de la NYSE. El resultado es que una serie de BRK-B a la que le falten una, dos o tres jornadas sale con stale=false. Es justo el hueco que el commit 3195908 quería cerrar, solo que para una forma de símbolo que no se contempló. Arreglo concreto: reconocer también los tickers con guion o punto que no terminen en un sufijo de plaza conocido, por ejemplo comprobando que la parte antes del guion sea alfabética y que el símbolo no termine en .MX, -USD, =X ni =F.

## B2b

Veredicto de la corrida 1: **pass_with_issues** (2 blocker/major, 1 minor)

- **[major]** El candado que decide qué series del SIE se publican mira SOLO el título, y la bandera `verified` del catálogo es código muerto. `kaizen_api/domain/rates.py:148-149` publica todo lo que `banxico.verified_ids()` confirme, y `_verify_now` (kaizen_api/providers/banxico.py:254) solo llama a `title_matches`, que exige que el título del SIE contenga las palabras de `tituloContiene`. Nada lee `item["verified"]` en producción (grep: solo aparece en tests/unit/b2b/test_banxico.py:296), y `periodicidad` y `unidad` se piden en `fetch_metadata` pero jamás se comparan contra lo que el catálogo declara. Consecuencia: en cuanto exista un BANXICO_TOKEN, los 10 ids marcados `verified: false` (TIIE 28, TIIE de Fondeo, los cuatro CETES, Bono M, inflación, subyacente y UDI) se publican como dato en vivo con `fallback: false` y sin nota, sin que ningún humano los haya confirmado. Esto es justo lo que el spec prohíbe ('SIE series ids other than SF43718/SF61745 must be verified against the SIE metadata endpoint in a test before use'), y el propio test del stream lo consagra: test_rates_mx_con_token_publica_solo_lo_que_el_sie_confirma afirma que cetes28 sale publicado. Verificado con un script propio usando `responses`: hice que el SIE devolviera para SF43936 el título 'Cetes a 28 dias, Tasa de descuento, Promedio mensual' con periodicidad 'Mensual' (el catálogo declara 'Semanal' y la nota dice que es el rendimiento de la subasta). El candado lo aceptó y /v2/rates/rf devolvió source='banxico', fallback=False, notes=[], values=[0.0715, 0.0712]. Es decir, una tasa de DESCUENTO mensual servida como la tasa libre de riesgo CETES 28, que es exactamente el error de convención act/360 rendimiento-contra-descuento que había que evitar, y encima sirve de rf para toda la app. Arreglo concreto: (a) en `_verify_now`, además del título, exigir que `meta['periodicidad']` coincida (comparación con `_fold`) con el `periodicidad` que el catálogo ya declara por serie, y rechazar si no; (b) agregar a `tituloContiene` las palabras que discriminan la convención, mínimo 'rendimiento' en los cuatro CETES y en bonoM10, y descartar títulos que contengan 'descuento' o 'promedio'; (c) publicar un id con `verified: false` solo marcándolo (nota en meta.notes nombrando los ids no confirmados a mano) en vez de servirlo como si fuera igual de sólido que el FIX.
- **[major]** /v2/news publica el cuerpo de la nota en `summary`, contradiciendo el contrato en cinco lugares. `kaizen_api/domain/news.py:286` copia `entry['summary']` tal cual, y para Yahoo esa cadena viene de `kaizen_api/providers/yahoo/news.py:19`, cuya última alternativa es literalmente `content.get('body')`, recortado a 250 caracteres en la línea 29. En mi corrida los 50 de 50 items traían summary, y 12 salieron cortados a exactamente 250 caracteres a media palabra: AFP '...fanning a tech rally.Enth', Insider Monkey '...positive earning', Investor's Business Daily '...reports tha', Proactive '...in early tradin'. Eso no es un sumario curado, es el arranque del artículo truncado, y se publica para AFP, Bloomberg, Reuters, Barron's, WSJ e Investor's Business Daily. Contradice: docs/api-v2.md:172 ('Solo titular y liga'), el docstring de NewsItem en schemas.py ('Titular y liga, nada más'), el docstring de kaizen_api/providers/rss.py ('nunca se reproduce el cuerpo de la nota'), el bloque v2 de kaizen_api/domain/news.py y el propio kaizen_api/data/feeds_es.json ('Nunca el cuerpo de la nota'). El handoff del stream también dice 'headline + link only'. El esquema congelado tolera el campo porque es `str | None`, pero la desviación NO quedó anotada en docs/requests/B2b.md, que es donde el stream sí registró sus otras tres desviaciones de contrato: nadie la va a revisar. Además el recorte a media palabra se ve feo en la UI. Arreglo concreto: en `build_items` poner `"summary": None` (o recortar a la primera oración completa y solo cuando el feed declare un dek, nunca desde `content['body']`), y quitar `content.get('body')` de la cadena del v2 sin tocar `_extract_note_item` del legado; si O prefiere conservar el adelanto, agregar el punto 4 a docs/requests/B2b.md y corregir los cinco textos que hoy afirman lo contrario.
- **[minor]** El procedimiento que debe cerrar la deuda del SIE promete más de lo que hace. `kaizen_api/providers/banxico.py:20` dice que la prueba 'comprueba título, unidad y periodicidad', y `kaizen_api/data/banxico_series.json` en `comoVerificar.queHace` dice 'además de revisar periodicidad y fechaFin'. En realidad tests/unit/b2b/test_banxico_live.py solo IMPRIME `unidad` y `periodicidad` (línea 64): la única aserción de clasificación es `title_matches`, más la comprobación de que SF43718 y SF61745 sigan confirmados. Verificado por grep: `periodicidad` no aparece en ninguna comparación del código ni de las pruebas, solo en `fetch_metadata` y en ese print. Importa porque esa prueba es el único ritual que el dueño va a correr cuando consiga el token, y de ahí saldrá la decisión de poner `verified: true`: si el SIE devuelve la serie equivocada con el título parecido, la prueba la imprime como 'OK'. Arreglo concreto: en test_banxico_live.py, además de `title_matches`, comparar `metadatos[sid]['periodicidad']` contra `catalogo[sid]['periodicidad']` con comparación sin acentos y dejar fuera de `confirmados` los que no cuadren, y marcar como sospechoso todo título que traiga 'descuento' o 'promedio' cuando el catálogo pide rendimiento. Si se prefiere no endurecer la prueba, corregir los dos textos para que digan lo que de verdad comprueba.

Veredicto de la corrida 2: **pass_with_issues** (1 blocker/major, 3 minor)

- **[major]** La verificación en caliente del catálogo del SIE es mucho más débil de lo que afirman el código y la documentación, y deja pasar exactamente el error que fue escrita para atajar. kaizen_api/providers/banxico.py::_verify_now solo llama a title_matches, que es un 'todas estas palabras aparecen como subcadena del título, sin acentos'. Ni compara periodicidad (el catálogo la guarda y nadie la lee: grep de 'periodicidad' solo la encuentra en fetch_metadata, en el print del test live y en los fixtures) ni compara unidad, aunque fetch_metadata ya la trae. Y tituloContiene de SF331451 es una sola palabra genérica, ['fondeo'], cuando el SIE tiene fondeo bancario y fondeo gubernamental; el de SF43881 es ['bonos','10'], que también acepta una serie de precio del mismo bono. Lo verifiqué con una sonda propia (demo_gate.py, detalle en commands): con token y el SIE simulado devolviendo esas dos series equivocadas, /v2/rates/mx respondió 200 publicando tiieFondeo=0.0749 etiquetado 'TIIE de Fondeo a un día' y bonoM10=1.025, es decir 102.5 % de rendimiento salido de un precio limpio en pesos multiplicado por el 0.01 de sieUnit, las dos con fallback=false y source=banxico. Es justo el porQue que escribe el propio catálogo: un id equivocado devuelve una serie real con el nombre equivocado y no se nota mirando el número. Se suma que el flag verified del JSON no lo lee ningún código de producción (solo tests/unit/b2b/test_banxico.py:296): la prueba test_rates_mx_con_token_publica_solo_lo_que_el_sie_confirma ya da por bueno que SF43936, marcado verified:false, salga publicado. Por eso comoVerificar.despues ('Un id que no pase se queda en false y su renglón no sale en /v2/rates/mx') describe algo que el código no hace, y el docstring de banxico.py línea 20 más comoVerificar.queHace dicen que la prueba live 'comprueba título, unidad y periodicidad' cuando test_banxico_live.py solo los imprime: su única aserción es title_matches sobre VERIFIED_IDS. No es blocker porque hoy no hay token y /health reporta banxico.configured:false, así que ninguna de estas series se publica y el respaldo de FRED es honesto; pero es la única barrera de los 10 ids sin verificar y cede el día que aparezca el token. Arreglo concreto, todo dentro de archivos de B2b: (1) en _verify_now exigir las tres condiciones, title_matches y _fold(info['periodicidad']) == _fold(cat[sid]['periodicidad']) y un cruce de unidad, sieUnit 'percent' exige 'por ciento' en _fold(info['unidad']) y sieUnit 'mxn' exige 'peso'; (2) apretar tituloContiene en banxico_series.json, SF331451 a ['fondeo','bancario'], SF43881 a ['bonos','10','rendimiento'], SF61745 a ['objetivo','politica monetaria']; (3) que test_banxico_live.py asserte unidad y periodicidad en vez de solo imprimirlas, para que los docstrings digan la verdad; (4) decidir una de dos y dejar el texto igual al código, o bien usable filtra también por catalog[sid]['verified'], o bien corregir comoVerificar.despues para que diga que quien decide es la consulta de metadatos y el flag es solo la revisión humana.
- **[minor]** En el respaldo de /v2/rates/rf, tenorDays es el único campo legible por máquina que nombra el plazo y dice el que se pidió, no el que se sirvió. Verificado con curl en 8160: ?tenorDays=364 devuelve tenorDays=364, convention 'simple_act360' y los mismos 35 puntos mensuales de IR3TIB01MXM156N que ?tenorDays=28, último 0.0679 en los dos casos. El aviso en meta.notes ya nombra el plazo pedido (bien resuelto en 585203c) y fallback va en true, así que un humano se entera; una máquina no, y el cliente que aplique la fórmula documentada rf_d = (1+y*T/360)^(d/T)-1 saca 4.86 % con T=28 y 4.69 % con T=252 días hábiles y T=364, o sea 17 pb de diferencia a partir de datos idénticos. Está declarado en open_issues y en docs/requests/B2b.md punto 3, y el Literal del contrato impide arreglar convention, pero tenorDays sí se puede arreglar sin tocar schemas.py: cuando fallback es true, devolver tenorDays=91, que es el plazo que de verdad tiene la serie, y poner en meta.notes que el plazo solicitado no está disponible mientras no haya token. Así el número y la etiqueta dicen lo mismo.
- **[minor]** Las rutas v2 no traen ninguna banda de cordura sobre lo que devuelve FRED, y el legado sí la tenía. domain/rates.py::get_rf (v1) descarta un valor fuera de 0.03 < rate < 0.20; el camino v2 (_fred_items y _item en domain/rates.py, _entry y UST_SPEC en domain/macro.py) multiplica por 0.01 lo que venga y lo publica. Lo verifiqué leyendo los dos caminos y confirmando en el JSON servido que no hay filtro: el mismo mecanismo que en mi sonda convirtió un precio de 102.5 en un 'rendimiento' de 1.025 aplica si FRED cambia la unidad de una serie, que es algo que FRED ha hecho al renumerar series. Hoy los fixtures dan valores correctos (9.16 %, 5.01 %, 3.88 %) así que no hay impacto, pero la clase de error es la que el encargo pedía cazar. Arreglo concreto: agregar un rango plausible por serie en FRED_MX_FALLBACK y en UST_SPEC (por ejemplo 0.0 a 0.30 en fracción para tasas, 5 a 100 para el VIX, 60 a 160 para el DXY) y, cuando el último punto se salga, no publicar el renglón y dejar la razón en meta.notes, igual que ya se hace cuando una serie no responde.
- **[minor]** No pude verificar los términos de uso de los dos medios elegidos en kaizen_api/data/feeds_es.json (expansion.mx y elfinanciero.com.mx) porque esta revisión corre sin red. El archivo documenta el criterio, la atribución y seis feeds descartados con su porqué, y descarta Investing.com justamente por pedir permiso escrito, así que el trabajo está hecho con cuidado. Pero queda un detalle que sí medí y que conviene conocer: además del titular y la liga, /v2/news publica un summary de hasta 280 caracteres del feed, y de Yahoo hasta 250 del campo content.summary (lo confirmé en el JSON servido, 0 con HTML crudo, varios a 250 exactos de Insider Monkey, IBD y Simply Wall St). El spec dice 'headline + link only' aunque el contrato sí tenga el campo summary, y el propio feeds_es.json promete 'nunca el cuerpo de la nota'. No lo reporto como defecto de código porque el contrato lo permite y el texto es el resumen que el editor publica, no el cuerpo. Lo dejo como cosa a confirmar antes de que esto sea público: revisar los términos de Grupo Expansión y El Financiero, y decidir si el summary se recorta más o se quita para las fuentes en español.

## B3a

Veredicto de la corrida 1: **pass_with_issues** (2 blocker/major, 2 minor)

- **[major]** fiscalYear equivocado por un ano en los estados trimestrales de la SEC cuando el ejercicio fiscal no es el ano calendario. kaizen_api/domain/statements.py:256 arma el periodo con _period_fiscal_year(end), que es puro calendario, mientras fiscalQuarter y form si salen del hecho de la SEC mas viejo (el fp del expediente que estrena ese cierre). Resultado: los dos campos se contradicen. VERIFICADO leyendo el companyfacts grabado de Apple (tests/fixtures/recorded/2026-09-22/http-GET-https-data.sec.gov-api-xbrl-companyfacts-CIK-53621d12c5afb391.json) y con curl a /v2/instrument/AAPL/statements?freq=quarterly: el cierre 2024-12-28 sale como fiscalYear 2024 + fiscalQuarter 1, cuando la SEC lo etiqueta fy 2025 fp Q1 en el 10-Q del 2025-01-31 (Apple lo llama primer trimestre del ejercicio 2025). Igual 2025-12-27, que sale 2025 Q1 y la SEC dice 2026 Q1. En la ventana devuelta, el ano 2024 queda como [Q2, Q3, Q1] y el 2025 tambien: cualquier UI que agrupe u ordene por (fiscalYear, fiscalQuarter) muestra el trimestre fuera de lugar y con el ano de mas. Afecta a todo emisor de EE. UU. con cierre distinto a diciembre (Apple, Microsoft, Walmart, Nvidia, Oracle). ARREGLO: en el bloque de periods de _sec_statements usar el fy del mismo hecho del que ya se toma fp y form, o sea "fiscalYear": int(source_fact.get("fy")) if (source_fact or {}).get("fy") else _period_fiscal_year(end). Lo comprobe contra los 9 cierres trimestrales y los 6 anuales de Apple: con ese cambio los anuales no se mueven (2020-09-26 a 2025-09-27 siguen dando 2020..2025) y los trimestrales quedan todos como los nombra la SEC. La ruta de Yahoo (statements.py:374) se queda con la heuristica, que ahi si es lo unico que hay, y ya esta dicho en la nota.
- **[major]** Cuando la SEC no responde, los estados de un emisor estadounidense caen a Yahoo (o a vacio) sin marcar fallback y afirmando algo falso. kaizen_api/domain/statements.py:403-415: _sec_statements devuelve None tanto cuando el simbolo no esta registrado ante la SEC como cuando la llamada fallo (get_companyfacts en providers/sec_edgar.py:185-201 devuelve None ante timeout, JSON malo o cualquier status distinto de 200, incluido el 429 de limite de tasa, que con EDGAR es comun). Las dos situaciones se tratan igual: se sirve Yahoo con meta.fallback en false y, si Yahoo tampoco trae nada, la nota "No hay estados financieros publicados para este simbolo.", que es una afirmacion sobre la emisora cuando el hecho real es que no se pudo leer a la SEC. VERIFICADO en el replay: cik_for('SPY') devuelve 0000884394 (si esta registrado), get_companyfacts('SPY') devuelve None porque esa llamada esta grabada como falla en el set base, y get_statements('SPY','annual') sale con source 'yahoo', fallback false y justo esa nota; el curl a /v2/instrument/SPY/statements lo confirma. Choca con la regla del contrato que el propio stream cita ("una fuente sustituta es fallback=true y nunca se muestra como en vivo") y con el criterio de procedencia honesta del proyecto. ARREGLO: distinguir los dos casos en get_statements. Si sec_edgar.cik_for(symbol) es verdadero y _sec_statements devolvio None, poner en el payload una llave fallback=True (el router de research.py ya puede pasarla a meta(...), que acepta fallback=) y una nota tipo "La SEC no respondio; estos renglones salen de Yahoo."; y cambiar la nota del payload vacio por una que diga que no se pudo consultar, en vez de afirmar que la emisora no publica estados.
- **[minor]** Un mismo ejercicio de RSU sale dos veces en los movimientos de la Forma 4, asi que el numero de acciones se duplica a la vista. kaizen_api/domain/screeners/insiders.py:202-216 (parse_form4) emite un renglon por cada nonDerivativeTransaction y cada derivativeTransaction, y en una vesting la SEC reporta las dos patas del mismo hecho: la adquisicion no derivada de acciones comunes con codigo M y la baja del derivado (la RSU) tambien con codigo M, por la misma cantidad y fecha. Las dos caen en type 'ejercicio' y quedan indistinguibles en la respuesta, porque el contrato no lleva securityTitle. VERIFICADO: en https://www.sec.gov/Archives/edgar/data/320193/000114036126037020/form4.xml (grabado en la capa 2026-09-22-b3a) las lineas son S 1438 D, M 30104 A (Common Stock), F 16228 D y M 30104 D (Restricted Stock Unit); el curl a /v2/insiders/AAPL muestra dos renglones identicos de 'ejercicio' 30104 para Newstead Jennifer el 2026-09-15, o sea 60,208 acciones para un movimiento de 30,104. Parseando los 20 expedientes salen 4 pares duplicados mas (2026-06-15 Newstead 30104, 2026-06-15 Borders 240, 2026-03-15 Newstead 60208). No contamina summary, que solo cuenta compra y venta. ARREGLO: al armar la lista de un expediente, descartar la pata derivada cuyo codigo cae en 'ejercicio' cuando en el mismo documento ya hay una nonDerivativeTransaction con el mismo codigo, misma fecha y mismas acciones; o quedarse solo con la pata no derivada para los codigos M/C/X, que es la que mueve acciones de verdad.
- **[minor]** El resumen de insiders cuenta solo los 40 movimientos que sobreviven al recorte, mientras la nota promete los ultimos 20 expedientes. kaizen_api/domain/screeners/insiders.py:302-306 ordena por fecha descendente, corta en MAX_ITEMS=40 y recien despues calcula openMarketBuys/openMarketSells, pero la nota que se agrega en el mismo flujo dice "Se leyeron los ultimos 20 expedientes". VERIFICADO parseando los 20 XML grabados de Apple con el propio parse_form4: dan 61 transacciones, de ellas 20 ventas en mercado abierto; la respuesta de /v2/insiders/AAPL reporta openMarketSells 18, porque las otras 2 quedaron fuera del corte. El lector entiende que en 20 expedientes hubo 18 ventas y fueron 20. Ademas el desempate del orden es por nombre de directivo invertido, asi que el corte puede partir un mismo expediente a la mitad. ARREGLO: calcular summary sobre la lista completa antes de items = items[:MAX_ITEMS] y decir en la nota que la tabla muestra los 40 movimientos mas recientes de esos expedientes, o bien redactar la nota como "de los N movimientos que se muestran" para que el numero y el texto digan lo mismo.

Veredicto de la corrida 2: **pass_with_issues** (1 blocker/major, 5 minor)

- **[major]** kaizen_api/domain/statements.py:256 (y :374): el fiscalYear del periodo se calcula de la fecha de cierre con _period_fiscal_year(end) en vez de tomar el campo 'fy' que la propia SEC trae en el hecho, mientras que fiscalQuarter SÍ sale de ese hecho ('fp'). Resultado: en cualquier emisora cuyo ejercicio no cierre en diciembre, el trimestre queda etiquetado con el año equivocado. VERIFICADO contra el companyfacts grabado y contra el endpoint: GET /v2/instrument/AAPL/statements?freq=quarterly devuelve {end: 2024-12-28, fiscalYear: 2024, fiscalQuarter: 1} y {end: 2025-12-27, fiscalYear: 2025, fiscalQuarter: 1}, cuando el hecho original (filed 2025-01-31 y 2026-01-30) dice fy=2025 y fy=2026. O sea la respuesta afirma que existe un 1T de FY2024 que cierra en diciembre de 2024, y en la MISMA base el cierre anual 2024-09-28 también se llama FY2024: 'FY2024' significa dos cosas distintas en una sola respuesta. En una emisora con ejercicio a enero (Walmart, Costco) se equivocan los CUATRO trimestres, no uno. Ninguna prueba cubre fiscalYear (tests/unit/b3a/test_statements.py solo revisa fiscalQuarter), por eso pasó. El anual NO está afectado (la regla de la primera semana de enero lo resuelve). ARREGLO: en el bucle de periods usar el 'fy' del source_fact cuando ese hecho sea el del expediente ORIGINAL del periodo, y si no caer a la regla de fecha; el criterio barato es la distancia entre 'filed' y 'end': fy_fact = source_fact or {}; sec_fy = fy_fact.get('fy'); filed = fy_fact.get('filed'); usa_sec = isinstance(sec_fy, int) and filed and 0 <= (date.fromisoformat(filed) - date.fromisoformat(end)).days <= 150; fiscal_year = sec_fy if usa_sec else _period_fiscal_year(end). El corte de 150 días es necesario: verifiqué que el cierre anual 2018-09-29 de Apple solo aparece como comparativo dentro del 10-K de FY2019 (filed 2019-10-31, fy=2019), así que confiar en 'fy' a ciegas rompería los anuales viejos. Agregar prueba: el trimestre de AAPL que cierra 2024-12-28 es fiscalYear 2025, fiscalQuarter 1.
- **[minor]** kaizen_api/domain/statements.py:367: en la rama de Yahoo, cuando la moneda de los estados es una unidad menor el renglón 'eps' se excluye a propósito del escalado (`if row_id != "eps" and divisor != 1.0`), pero el campo currency de la respuesta sí se normaliza a la moneda mayor. VERIFICADO llamando _yahoo_statements("TEST.L", "annual", "GBp") con un marco sintético de 1,000,000 de ingresos y 250 de UPA: la respuesta sale currency 'GBP', revenue 10000.0 (bien, peniques a libras) y eps 250.0 (mal: sigue en peniques, 100 veces la cifra que corresponde a la moneda declarada). Alcance estrecho, porque Yahoo suele publicar financialCurrency='GBP' aunque cotice en GBp, pero si algún emisor llega con financialCurrency='GBp' la UPA sale 100x inflada junto a ingresos correctos. ARREGLO: quitar la excepción de 'eps' (la UPA viene en la misma unidad que el resto del estado, así que se divide igual), o, si la excepción era deliberada, dejarlo escrito y anotarlo en meta.notes; hoy no hay comentario que lo explique.
- **[minor]** kaizen_api/domain/fundamentals.py:395 _weekly_returns: el rendimiento se guarda con la fecha FINAL del intervalo y nada registra su fecha inicial, así que compute_beta empareja por esa sola fecha. Si una de las dos series trae un hueco, un rendimiento de 2 semanas se empareja con uno de 1 semana y la beta sale sesgada en silencio. VERIFICADO con series fingidas: con el referente completo la beta da 2.0000 exacta; borrando UNA observación del referente a media ventana da 2.0074 con 59 observaciones y sin ninguna nota. El error es chico aquí, pero crece con el tamaño del hueco y hoy no hay nada que lo detecte. ARREGLO: que _weekly_returns devuelva {fecha_final: (fecha_inicial, rendimiento)} y que compute_beta solo empareje cuando coinciden LAS DOS fechas, descartando el resto y anotando cuántas observaciones se descartaron. Está latente mientras history.get_series (B2a) siga en NotImplementedError, pero se activa el día que esa costura aterrice.
- **[minor]** kaizen_api/domain/screeners/insiders.py:204 parse_form4: un mismo evento de vesting se reporta dos veces. La Forma 4 lo anota en las dos tablas (nonDerivativeTransaction con código M por las acciones que se reciben y derivativeTransaction con código M por los títulos que se extinguen) y el parser emite un renglón por cada una, las dos con type 'ejercicio', la misma fecha y las mismas acciones. VERIFICADO leyendo el XML crudo de los expedientes grabados de Apple: la forma del 2026-06-15 trae nonDerivative M 30,104 + F 16,238 + derivative M 30,104, y /v2/insiders/AAPL devuelve 'ejercicio 30104' dos veces; lo mismo el 2026-03-15 con 60,208 duplicado. El resumen no se contamina (solo cuenta compra/venta de mercado abierto), pero quien lea la lista ve 60,208 acciones ejercidas donde se movieron 30,104. ARREGLO: al armar la lista, descartar el renglón derivado con código M cuando ya existe uno no derivado con la misma fecha y las mismas acciones, o etiquetarlos distinto (por ejemplo 'ejercicio' contra 'ejercicio (derivado)') para que no se lean como dos operaciones.
- **[minor]** meta.stale sale siempre en false en los cinco endpoints: kaizen_api/routers/research.py, insiders.py y events.py llaman a provenance.meta() sin pasar stale, y el valor por omisión es False. En /v2/instrument, que es el único que anuncia delayMinutes 15, eso significa que una cotización de un viernes leída un lunes se entrega con stale=false, que es justo lo que ese campo existe para evitar. Verificado leyendo las tres llamadas a meta() y confirmando que provenance.py:67 solo refleja el argumento. Atenuante real: NINGÚN router del repo calcula stale todavía y el api-v2-spec.md no fija umbral, así que esto es deuda del contrato más que de B3a; lo reporto porque B3a es el primer stream que sirve cotizaciones con retraso declarado. ARREGLO mínimo dentro de sus archivos: en el router de instrument, stale = as_of is None or (ahora - as_of) > delay_minutes + margen de una sesión, y dejar escrito el umbral en docs/requests/B3a.md para que el resto lo copie.
- **[minor]** kaizen_api/domain/events.py:_symbol_events: en los renglones de tipo 'earnings' el campo currency se deja en null a propósito (solo se llena para exDividend y dividendPay), pero esos renglones sí traen 'estimate', que es un consenso de UPA en una moneda concreta. Verificado con GET /v2/events?symbols=AAPL,WALMEX.MX: sale {symbol: WALMEX.MX, type: earnings, estimate: 0.7129, currency: null} junto a {symbol: AAPL, type: earnings, estimate: 1.9812, currency: null}, o sea dos números en monedas distintas presentados sin distinguirlas. El esquema ya tiene el campo y normalize_currency(info['currency']) ya está calculado dos líneas arriba. ARREGLO: poner currency también cuando estimate no es None (la moneda de cotización, que es la que usa Yahoo para el consenso de UPA).

## B3b y B3c

Sin revisión independiente. Antes de que una feature se apoye en la valuación, el DCF, el momentum o
los tres screeners, conviene correrles la misma revisión adversaria que a los demás: el encargo y el
prompt del revisor están en `docs/overhaul/workflows/fase2.js`.
